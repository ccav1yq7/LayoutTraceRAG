# 客服交付行为完善

本轮围绕现有 FixedRAG 与前端完善，不新增模块。

- 内容状态与配图分离：保留 Writer 的 answered/partial，仍要求独立核验通过；不再因 B2 无图片强制 partial。可选图片不可用保留提示和审计 warning。真实内容不完整仍为 partial，不批量把历史结果改为 answered。
- 提示升级 fixed-rag-v7：简单问题直接回答，操作过程才分步骤；避免摘要与步骤重复。不为状态评分添加无关图片。用户明确需要视觉指导或任务依赖视觉定位时，Writer 与 Verifier 都须检查这一要求，不能用文字完整性掩盖缺失。该判断依赖模型，仍需真实场景验收。
- 客服失败提示分开处理：资料不足时提供补充现象、提示或照片的入口；模型服务/格式错误提示稍后再试，不把内部错误码当成客服内容。来源或可靠性无法确认时保留拒答，并给出补充情况或联系售后的建议。没有声称已替用户联系售后。
- 前端展示既有 followup_question 字段，用户可在现有输入框继续回复。内部 error/failure_detail 和评分状态保留，供追踪；用户回答区不输出错误码。

来源授权、图片来源绑定、未知证据拒绝、必需核验与完整性检查均保留。未增加自动重试：原 B2 流程通常已占满三次调用预算，恢复策略还需按实际客服与评测两种调用入口单独设计；不能偷偷扩充预算或改变冻结实验协议。本轮不宣称稳定性和整体 M3 已验收。

旧 300 条预测与评分不改写。新行为与旧无图 partial 标签不可直接混合统计。测试证据：`artifacts/shopguide/customer-flow-tests.xml`；真实定向复验：`artifacts/shopguide/customer-flow-v7/`。

## 验证结果

前端构建及 2 项测试通过，Ruff/Mypy 通过。后端 ShopGuide 全集首次 226 通过、1 个旧断言失败：Agent 预算测试要求无图 partial，与新内容状态规则冲突；已更新该断言并单独重跑 Agent 测试，预算三次断言仍保留。

真实两题复验：1 answered、1 Writer MODEL_TIMEOUT；使用 5 次请求，无自动重试，累计 **843/870**，剩余 27 次。源码冻结哈希与账本计数核对通过。此次只验证定向行为，不是新完整评分；Writer 超时仍需后续处理。

Agent 定向回归最终 **25 项全部通过**（67.70 秒），证据 `artifacts/shopguide/customer-flow-v7/agent-tests.txt`。全集其余 202 项加该文件 25 项均已通过；未把首次失败的 XML 改写为全绿。
