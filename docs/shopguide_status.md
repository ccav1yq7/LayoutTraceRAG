# ShopGuide 实施状态

> 2026-09-10 最新状态：本地 Workbuddy `deepseek-v4-flash` 完整复测与六组评分已完成，300 条预测，新批次 666 次、累计 799/870 次请求。见 [最终复测记录](m03_workbuddy_retest.md)。下文此前未重跑/待预算的描述为历史阶段状态；M3 稳定性、效果和发布验收仍未通过。

最新 v5 复测已中止：无图核验的空对象 schema 导致 HTTP 400。已修复为 v6，
真实 B1 验证通过；完整复测仍待增加预算，见 [恢复记录](m03_v5_retest_recovery.md)。

最新核验修复：[固定键协议与复验](m03_verifier_repair.md)。本轮 16 请求、238 项回归
通过，旧重复 ID 案例已验证修复；complete=false 仍拒绝，完整新基线未复测。

覆盖率第一轮修复：[定位与复验](m03_coverage_investigation.md)。本轮 47 请求、
222 项测试通过；4 个定向 B2 失败样例变为 3 个带图回答，两个 B1 正常拒答仍不计覆盖率。
这不是全量覆盖率结果，M3 继续未验收。

2026-09-10 当前结论：**本地演示链路可用，工程实现已推进较多；完整计划和最终验收尚未完成。**
停止扩展范围，回到原模块逐项补齐。最新分类、代码证据和关闭条件统一见
[PLAN 验收清点](plan_acceptance_audit.md)；发布证据复核仍为 **do_not_release**。

最新 50 题实验：[完整运行报告](m03_val50_baseline.md)。300 条真实预测、六组完整评分，
595 次请求；执行已完成，B2 大量校验失败，M0–M3 整体验收仍未通过。

此前 M0–M3 收尾进展：完整文字/区域评分接入和隔离参考验证、真实 BGE 522 页/50 题检索与两题三协议真实 B1/B2 pilot 已完成，
详见 [当前收尾记录](m03_completion.md)。当时完整 50 题问答尚未运行；现已完成运行，B2 稳定性及最终验收仍待完成。

以下按时间保留历史记录；“下一工程模块”等当时表述不再作为当前执行安排。

2026-09-09：M1–M5 本地工程链路已实现。全量 **151 项 Python 测试**、
**2 项前端单元测试**和 **7 项 Chromium 浏览器测试**通过，Ruff/Mypy 与前端构建通过。

M5 交付 FastAPI + React/Vite：商品选择、图文步骤、受授权的原图/来源查看、
补图、步骤追问、SSE、取消、刷新恢复、反馈、确认模拟工单。
真实 DeepSeek 图文问答与补图成功，模型为 `deepseek-v4-flash-vision-exp`。
失败记录保留，检索仍用测试 hash/RRF，不作为正式效果成绩。

参见 [M5 启动、验证与剩余门槛](m5_api_frontend.md)。下一工程模块为 M6 ECom
原生环境适配；M5 全部产品场景联验、真实 BGE、完整官方评分和 M7 部署尚未完成。
未提交、推送或部署，也未连接真实业务账户。

历史交付：[M2](m2_data_retrieval.md)、[M3](m3_fixed_rag.md)、
[M4](m4_agent_sessions.md)、[DeepSeek 接入](deepseek_validation.md)。
外部依赖问题保存在 [独立问题登记](issues/external_blockers.md)。

M6 更新：原生 ECom MCP、官方评分桥接、独立 trial、超时清理和冻结清单已实现。
新增 22 项测试，全套 173 项通过；自制订单原生写入及负向评分验证通过。
已获批准并执行 dev 任务 1 的两种方法：总计 20 次模型请求、100848 报告 tokens。
固定策略完成并通过原生评分，自适应策略在共享预算用尽时中止，不能比较方法
优劣。详见 [真实冒烟与剩余门槛](m6_deepseek_dev_smoke.md)。
原生图片资源仍为 5/19 可用；未把自制 smoke 当作正式成绩。

M7 更新：全量 184 项 Python 测试、2 项前端单测、候选容器 9 项浏览器测试通过。
已完成 SIGKILL 恢复、同版本冷备份/恢复并读取原图；修复 Vitest 漏洞及快速切换
型号时发送请求的竞态，并保存依赖
审计及 69 组件清单。当前 release-check 为 **do_not_release**，并未公开发布。
剩余性能、正式评测、生产认证、供应链/许可与跨版本回滚见
[M7 运行手册](m7_operations_release.md)。本轮未追加模型费用。

2026-09-10 供应链后续：全量 193 项 Python 测试、新镜像 9 项浏览器测试通过。
已验证官方基础内容并完成 OS 扫描；移除运行镜像构建工具并更新系统后，可修复
高危/严重匹配 39→0，仍有 60 项待处理。发布仍不通过，见
[供应链复验与复现命令](supply_chain_validation.md)。

继续核查：候选基础环境已升级至 Debian 13，SQLite/zlib 对应严重匹配消失；
最新原始扫描为 3 CRITICAL、51 HIGH。3 个严重匹配中两项不满足当前镜像的
模块/架构条件，仍保留原始扫描，未自动豁免。新镜像浏览器与备份回退验证通过。
