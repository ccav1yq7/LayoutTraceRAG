# 外部问题登记

50 题后续：750 请求大实验已获批并完成，实际 595 次，预算不再阻塞这份运行。
595 次中 594 次有 usage，另 1 次网关/响应失败缺少用量；大量其他失败发生在本地
结构/组装校验，不能归因于历史 GPT 故障。见 [完整结果](../m03_val50_baseline.md)。

2026-09-10 后续：NLGEval/Stanford/Java 8 参考资源现已准备并通过完整指标合成验证；
PM209 正式评分入口已接入，真实 BGE 局部验证通过。EXT-003 的评分环境部分已解决，
资料许可仍未关闭；两题真实 pilot 已获批，累计本轮 30 请求；完整 50 题大实验预算尚未批准。详见 [M0–M3 收尾](../m03_completion.md)。
下列复核及阶段记录保留历史语境。

独立记录外部依赖故障。当前停止扩展范围，按 [PLAN 验收清点](../plan_acceptance_audit.md)
补齐原模块；可独立完成的收尾工作不代表前置模块验收通过。不得用 fake 结果替代真实结果。

2026-09-10 口径复核：EXT-001 仅针对原 GPT 服务，当前替代模型已有历史成功记录；
EXT-002 的 5/19 是先前探测，本轮未重测。EXT-003 中 ECom 独立环境接入已实现，
尚未关闭的是 NLGEval/许可；此外 PM209 正式文字评分代码仍未接入，属于实现缺口，
不能全部归因于外部条件。真实 BGE 当前环境未准备好，也不是已证实的上游服务故障。

| ID | 状态 | 问题 | 影响范围 | 恢复条件 |
|---|---|---|---|---|
| EXT-001 | OPEN | gpt-5.6-terra 转发服务上游拒绝访问，502/503 | 真实 Planner/VLM/Verifier smoke、模型效果评测 | 服务端凭据/渠道修复后，真实能力 smoke 通过 |
| EXT-002 | OPEN | ECom 19 个图片链接上次探测仅 5 个可读取 | ECom 完整原生评测 | 全部资源复核并建立可复现来源记录 |
| EXT-003 | OPEN | 资料授权尚未完成；NLGEval 与 ECom reference 接入已有证据 | 公开资料展示/分发 | 许可检查通过 |

EXT-001 的请求对照及请求 ID 见 [诊断报告](../model_service_diagnosis.md)。
2026-09-09 用户明确要求单独记录此类问题并继续后续模块，因此继续 M2 的数据、存储、检索开发；本轮不反复请求故障服务。

M3 补充（2026-09-09）：固定 B1/B2 工程链路和区域包装已完成，不再阻塞后续
工程推进。仍需真实 LLM 与 pinned BGE 验证、完整 NLGEval 文字 parity、50 题
val 冒烟及实测基线冻结，才能关闭 M3 正式验收。本轮没有重复调用故障端点。

验证复查（2026-09-09）：真实 Planner/Vision 两次探测均返回 503，EXT-001
保持 OPEN；本地评分/检索环境预检确认 nlgeval、sentence_transformers 尚未
安装，BGE 缓存在本轮检查位置未发现。工程验证全部通过，详情及新的请求 ID
见 [M3 验证报告](../m3_verification.md)。未将 fake 成功视作真实服务恢复。

当前替代模型更新：用户指定 deepseek-v4-flash-vision-exp，已通过真实读图与
B2 三次调用。EXT-001 对原 GPT 服务仍保持历史记录，已不阻塞当前 DeepSeek
调用。真实 BGE、NLGEval 与正式基线仍需准备。见 [验证结果](../deepseek_validation.md)。

M4 工程更新：Agent 与共享工具固定控制器的真实 DeepSeek 小样例及追问已完成，
失败记录保留。该样例仍使用 hash/RRF，不关闭真实 BGE、完整评分、多任务
效果和浏览器门禁。详见 [M4 交付记录](../m4_agent_sessions.md)。

M5 工程更新：API/React 核心浏览器路径和真实 DeepSeek 补图已通过。完整产品
场景联验缺口与补图失败/修复记录见 [M5 交付](../m5_api_frontend.md)，不归因于
历史 GPT 服务 503，也不关闭 BGE/官方评分问题。

M6 复查：固定原生参考环境、MCP 和官方 scorer 工程接入已通过，不再将其描述
为未接入。2026-09-09 图片链接仍为 5/19 可用，EXT-002 保持 OPEN；EXT-003 的
NLGEval/资料许可事项未关闭。真实 ECom 模型运行尚未开始，需 PLAN 16.3 预算
确认；这不是服务返回 503。见 [M6 交付](../m6_ecom_native.md)。

M6 预算更新：用户已批准并执行 20 次模型请求的 dev 冒烟，额度未超出。固定
策略完成，自适应策略中止；证据支持共享预算耗尽，未看到 503 记录，不计作
EXT-001 复发。原日志嵌套异常与比较限制见 [真实运行记录](../m6_deepseek_dev_smoke.md)。

M7 更新：Docker Hub 拉取在本机超时；使用已有 Python 3.12 镜像缓存完成了本地
候选验证，未将其视为供应链已认证的发布镜像。正式评测、资料许可与真实 BGE
问题仍未关闭。发布证据审阅结论为不发布，详见 [M7 记录](../m7_operations_release.md)。

2026-09-10 供应链：已通过官方 Registry API 核对旧/新基础镜像配置和层内容，
解决此前仅有第三方缓存来源记录的问题。完成新镜像 OS 扫描和第一轮更新，
仍有 60 个高危/严重匹配没有现成修复，需要上游修复或有证据的逐项研判。
未忽略这些结果；发布者签名等门槛仍未关闭。详见 [复验](../supply_chain_validation.md)。
