# ShopGuide 开发计划与测试规范

> 面向已购商品的图文使用指导与售后问答 Agent
>
> 基于 `ccav1yq7/ShopGuide` 渐进改造；保留旧版长视频 RAG，新增商品、说明书、原图证据和多轮工具决策。

| 项目 | 内容 |
|---|---|
| 文档版本 | 1.2（新增意图候选检索与 RAG 优化计划） |
| 编制日期 | 2026-09-09 |
| 状态 | 实施中；工程与真实 smoke 已记录，发布门禁未通过 |
| 产品名称 | ShopGuide |
| 仓库 | `ccav1yq7/ShopGuide`，第一版不强制更名 |
| 源码审阅基线 | `fe4050d1c228c0dd4ecd5d37c4f77b2b9dd395d2` |
| 核心评测 | PM209 / MPMQA：说明书检索、文字回答、原始区域选择 |
| 交互评测 | ECom-Bench：多轮客服、工具使用、模拟业务任务完成 |
| 实施原则 | 先跑通数据与评测，再构建 Agent；先保证来源与型号正确，再优化模型 |

**文档口径。**“必须”表示验收约束，“建议”表示可经架构决策记录调整的设计。“计划新增”目录、接口、命令与测试均需开发，不能视为当前仓库已经支持。文中的预算、工期、覆盖率和性能门槛是拟议目标，不是实测结果。编制时已静态审阅相关源码与官方资料，尚未完整下载 PM209、运行两套基准、验证模型部署或修改远端仓库。

**第一版的核心交付：用户选中自己的商品后，助手能够查到适用说明书，按步骤回答，并显示来自真实资料的原始图块；用户继续追问时，助手能够沿用正确上下文、补查证据或澄清，而不是编造图像和操作方法。**

---

## 当前验收状态（2026-09-11）

最新测试：第三轮真实验收在累计 1345/1345 时停止，执行了 16/24 条消息，剩余 8 条未执行；不标全量通过。执行前后端 287 项及前端检查通过；测试后已离线修复只读工具多余 reason 参数问题，38 项相关回归通过，尚无额度真实复验。见 [第三轮报告](docs/customer_acceptance_results_v3.md)。

9 月 11 日修复：未选商品直接澄清、意图 continue/update、重复属性审计、Planner 澄清契约、正常拒答、限制说明核验、纯查询收尾与检索缺图提示已完善并定向复验。累计 1263/1345，剩余 82；最新版本尚未全量重跑，见 [修复记录](docs/acceptance_repairs_20260911.md)。

最新完整复验：当前版本已重跑 12 场景/24 条消息，整体仍未通过。原图问答、工单闭环、暂停撤回、换商品、取消恢复与移动端已有通过证据；意图候选/迁移/属性、排查拒答包装及 Planner 输出仍阻断部分场景。本轮 102 请求，累计 1159/1345，详见 [第二版完整验收](docs/customer_acceptance_results_v2.md)。

收尾完善：纯暂停/撤回现在直接确认，避免重复旧指南；明确规则咨询缺资料时不再要求重复补型号。两条真实会话及浏览器复验通过，本轮 11 请求，累计 1057/1158。完整场景尚未重跑，见 [收尾记录](docs/customer_closure.md)。

验收修复进展：待确认诉求继承、业务回执衔接、意图受限修复/诊断、视觉需求与原图预算、混合核验子问题及订单项传递已修复；两轮定向真实复验使用 76 次，累计 1046/1158。暂停撤回、原图、混合回答、上传对照及创建→查询已有成功证据，完整 12 场景尚未重跑，不标整体验收通过。见 [修复记录](docs/customer_acceptance_repairs.md)。

最新真实联验：路由恢复后已执行 12 场景/24 条消息，**整体未通过**。已验证商品选择/切换、工单确认防重复、权限与取消恢复；意图失败/多余澄清、待确认 goal 继承、混合核验、原图链路与缺图提示仍未通过。模型图像输入本轮为 0，不能声明多模态场景已验收。累计 970/1158，详见 [验收结果](docs/customer_acceptance_results.md)。

全链路验收最新状态：追加 288 次预算已获批；实际尝试 3 条场景消息及 1 次诊断后，确认配置模型返回 HTTP 502 / unknown provider。累计 874/1158，后续 21 条消息暂停，尚无完整真实场景通过；恢复路由后继续，见 [验收记录](docs/customer_acceptance.md)。

历史验收准备：真实 BGE 测试索引、12 场景/24 消息清单及请求计数服务已准备，见 [验收记录](docs/customer_acceptance.md)。旧累计 870 次额度已耗尽；新的真实阶段尚未运行，需批准冻结清单的新预算，不标为验收完成。

需求增补：用户提供“多路粗判→候选融合→精排→上下文判决”方案，已分析为 **RAG 前置的意图候选检索层＋现有证据 RAG 的上下文优化**，登记在 §8.7–8.11、§12.10、M2/M3/M4 及 IR 测试矩阵。第一阶段规则/槽位/样例召回/融合/通用重排与 Judge 接入已实现；BERT 已用 342 条自编扩充样例完成新版微调，并按用户授权在本地真实客服启用；真实数据验收未完成（见 [训练与实装](docs/bert_expanded_installation.md)）；真实本地 BGE 六例开发探针已跑，Judge 尝试失败，完整改造未验收。见 [阶段实现记录](docs/intent_candidate_implementation.md)；不改变主从架构、暂停人工交接及业务确认边界。

2026-09-10 需求增补：用户提出独立意图理解、多诉求及多轮变化跟踪，已纳入 FR-15、§12.4–12.8、M4/M5 和 INT 测试矩阵。**已实现首版意图库、变化校验、会话/Agent 接入并通过定向验证；FR-15 整体未验收**；这是原客服目标下的明确需求补充，不表示 M3 已验收，也不另设顺序完成的新模块。

客服交付行为已完善：内容状态与可选配图分离、按问题组织回答、区分资料不足与服务异常并展示后续补充提示。见 [客服功能记录](docs/m03_customer_flow.md)。可靠性恢复策略及完整产品场景验收仍未关闭。

本地 Workbuddy 多模态路由完整复测已完成：50 题三协议 B1/B2，300 条预测、六组完整评分；新批次 666 次请求，含中止与预检累计 **799/870**。B2 覆盖率为 72%/54%/60%，旧配置为 28%/18%/16%；模型路由与修复同时改变，覆盖率不是准确率。仍有 53 条拒答、43 条错误，B2 稳定性及效果未验收。见 [完整复测记录](docs/m03_workbuddy_retest.md)。

原 v3 基线（595 次请求）、v5 中止及定向修复记录作为历史证据保留；本轮关闭的是完整重跑与评分缺口，不是整个 M3 的退出条件。实验前 250 项测试通过，不能替代未执行的验收任务。

**本地演示链路可用，工程实现已推进较多；完整计划和最终验收尚未完成。**
后续工程提前推进不表示前置模块验收通过。停止扩展范围，按原模块收敛未完成项。
逐项代码证据、缺口分类及关闭条件见 [PLAN 验收清点](docs/plan_acceptance_audit.md)。
2026-09-10 收尾更新：完整文字/区域评分、真实 BGE 522 页建库和 50 题检索已验证；
两题 pilot 及定位先前使用 30 请求；后续完整 50 题三协议 B1/B2 使用 595 请求，
运行和评分已完成，B2 稳定性与效果仍未验收。历史工程记录见 [M0–M3 收尾记录](docs/m03_completion.md)。

| 范围 | 已有局部实现与验证 | 未关闭验收 |
|---|---|---|
| M0–M5 | 数据、检索适配、图文问答、Agent、API/前端和真实模型小样例 | B2 稳定性及效果验收、完整 test、公平 Agent 对照、全部产品场景 |
| M6 | 原生工具适配、隔离运行器、评分/报告和已获批的 20 请求 smoke | 完整任务重复实验、等条件基线及逐 trial 最终状态审计 |
| M7 | 本地容器、部分安全修复、备份恢复和供应链扫描 | 剩余漏洞处置、生产认证、真实性能、许可、新环境与最终发布验收 |

测试通过数只证明被测内容；以下数字为分阶段记录，不替代 §17–19 和 §24 的退出条件。
发布证据复核仍为 **do_not_release**。历史结果与本轮复验结果分开记录。

## 历史实施记录（2026-09-09 至 2026-09-10）

M1–M5 本地交互链路已实现，M6/M7 工程与运维验证已推进；当前全套
**193 项 Python 测试**、2 项前端单元测试、
9 项真实浏览器测试通过（含 M7 两项竞态回归）。M5 新增 FastAPI、React/Vite、授权原图、私有补图、
SSE、取消恢复与确认交互，详见 [M5 交付记录](docs/m5_api_frontend.md)。

真实模型使用 `deepseek-v4-flash-vision-exp`，已完成浏览器图文问答与补图重试，
严格核验通过；首轮 partial 和补图失败也保留。检索仍为工程测试模式，真实
BGE、完整官方评分与多任务效果实验尚未验收。

M5 的浏览器核心路径已通过；完整 E-01 至 E-12 联验仍有缺口，包括不同版本
实拍冲突和浏览器连接时进程崩溃恢复，不能将核心 smoke 当作全部退出条件。
M6 原生适配工程已实现（独立参考环境、原生 MCP、官方评分、trial 隔离、
watchdog 和冻结报告）；新增 22 项测试通过，全套 173 项。已按批准的 20 次请求
上限执行 2 个真实 dev trial：固定策略完成，自适应策略中途中止，不能当作公平
对照；详见 [M6 真实冒烟记录](docs/m6_deepseek_dev_smoke.md)。全量评测仍未完成。问题继续放在
[独立问题登记](docs/issues/external_blockers.md)。

M7 已验证本地非 root 容器、SIGKILL 后恢复、消息重放幂等及离线备份恢复，
并修复 Vitest 依赖漏洞与切换型号时快速发送的竞态。当前发布证据检查返回 **do_not_release**：正式评测、
公平对照、真实性能、生产认证和供应链/许可等门槛仍未满足。详见
[M7 交付与运行手册](docs/m7_operations_release.md)。

2026-09-10：继续 M7 的供应链验收，已核对官方基础镜像内容，完成 OS/语言包
扫描并升级候选镜像。有修复版本的高危/严重匹配从 39 项降至 0；仍有 60 项
未修复/待研判匹配；继续升级至 Debian 13 后降至 54 项（3 严重/51 高危），
SQLite 严重项已修复，门禁继续不通过。新增 9 项测试、容器 9 项浏览器测试通过；
详见 [供应链复验](docs/supply_chain_validation.md)。

---

## 目录

- [01. 产品定位、范围与成功标准](#s01)
- [02. 现有代码评估与迁移边界](#s02)
- [03. 技术选型与架构决策](#s03)
- [04. 总体架构与目标目录](#s04)
- [05. 数据接入、授权与防泄漏](#s05)
- [06. 数据模型与证据契约](#s06)
- [07. 说明书、原图与视频入库流程](#s07)
- [08. 检索、重排序与型号约束](#s08)
- [09. Agent 状态机与决策规则](#s09)
- [10. 工具接口与执行语义](#s10)
- [11. 图文答案生成与强制核验](#s11)
- [12. 多轮会话与任务恢复](#s12)
- [13. API、事件流与前端交互](#s13)
- [14. 安全、隐私与内容边界](#s14)
- [15. PM209 评测实施协议](#s15)
- [16. ECom-Bench 评测实施协议](#s16)
- [17. 测试分层与详细测试矩阵](#s17)
- [18. 测试执行、统计分析与质量门禁](#s18)
- [19. 分阶段开发流程与交付物](#s19)
- [20. 开发环境、配置与命令约定](#s20)
- [21. CI/CD、部署、运维与回滚](#s21)
- [22. 可选的 SFT 与强化学习路线](#s22)
- [23. 风险登记与资源计划](#s23)
- [24. 最终验收、交付清单与协作规则](#s24)
- [25. 资料来源与版本登记](#s25)

---

<a id="s01"></a>
## 01. 产品定位、范围与成功标准

### 1.1 要解决的任务

ShopGuide 面向电商售后中的**商品使用指导**，优先选择说明书和测试数据能够覆盖的 3C 数码、家电商品。不是通用购物推荐，也不是现实京东系统的内部架构复刻。

核心任务链：

```text
识别当前商品 → 确认型号/版本 → 查阅适用资料 → 组织操作步骤
             → 选择来源原图 → 核验图文依据 → 接受追问与执行反馈
```

用户可通过模拟订单选中商品，也可直接输入型号。系统需要区分“用户买了什么”“用户正在咨询什么”和“用户照片中是什么”，三者存在冲突时应确认，不得自动合并。

### 1.2 功能需求与优先级

| 编号 | 优先级 | 需求 | 验收证据 |
|---|---|---|---|
| FR-01 | P0 | 查询当前用户的模拟订单，选择商品及具体型号 | 订单隔离与型号歧义测试 |
| FR-02 | P0 | 导入说明书页面/文档并保留来源、版本、区域坐标 | 数据审计报告和可回溯资产 |
| FR-03 | P0 | 在适用资料范围内检索文字、页面和图块 | 检索结果与作用域测试 |
| FR-04 | P0 | 根据证据输出分步骤说明与原始配图 | PM209 预测及图文答案快照 |
| FR-05 | P0 | 延续多轮上下文，支持指代、补拍、切换商品 | 多轮集成与前端测试 |
| FR-06 | P0 | Agent 自主决定查询、看图、补查、澄清或回答 | 执行轨迹，不以工具数量证明能力 |
| FR-07 | P0 | 引用、型号、资产权限及支持性强制核验 | 正反例测试与拒绝日志 |
| FR-08 | P0 | 完整运行 PM209 官方协议适配评测 | 版本化预测文件、指标与环境清单 |
| FR-09 | P0 | 接入 ECom-Bench，执行其允许的模拟读写工具 | 官方评分结果与完整实际调用记录 |
| FR-10 | P0 | 记录成本、耗时、错误、证据和工具执行轨迹 | 可重放 run 与结构化报告 |
| FR-11 | P1 | 用户上传商品照片/截图后进行针对性观察 | 图片有效性、隐私和观察准确性测试 |
| FR-12 | P1 | 接入授权教学视频、返回帧图和时间引用 | 视频定位与来源测试；不并入 PM209 主分数 |
| FR-13 | P1 | 模拟转人工、创建售后工单并保存咨询摘要 | 写操作确认、幂等与恢复测试 |
| FR-14 | P2 | 小模型 SFT、工具策略 RL、高级视觉检索 | 独立训练集、消融、未见任务评测 |
| FR-15 | P0 | 独立意图理解：多诉求、指代否定、纠正、暂停恢复、歧义与原文溯源；意图不代表业务授权 | §12.4–12.8、INT-01 至 INT-12、独立真实多轮评测 |

P0 必须覆盖核心图文问答和两条评测线。用户后续确认的八项客服能力按 §1.2.1 纳入目标范围；FR-13 的本地售后闭环进入当前实施顺序，真实业务系统接入仍受外部条件限制。其他 P1 可在首个可用版本后完善。

### 1.2.1 已确认的八项客服能力（用户确认，2026-09-10）

最新范围调整：先补本地工单查询，人工交接暂不做；人工能力仍留在目标清单，未标完成。

用户确认以下八项均纳入目标范围。图片理解是指南和排查的支撑能力，不单独扩展业务入口；商品购买推荐不在本轮范围。下表区分目标和实际完成度，确认范围不等于确认已实现。

| 能力 | 下游职责 | 当前代码边界 | 关闭条件 |
|---|---|---|---|
| 商品确认 | 查询已购商品、消歧并由用户确认型号 | 已有模拟订单与候选查询；不能自动替用户选商品 | 跨用户隔离、同品牌多型号、切换后状态与证据隔离通过 |
| 操作指南 | 基于说明书解释使用、安装、配对、设置 | 已有文字/原图步骤及核验，稳定性仍待验收 | 简单问题不强制步骤或配图，必要视觉指导有来源，回答可执行且不编造 |
| 故障排查 | 理解现象、补查、追问、根据反馈调整建议 | 已有资料/照片工具与多轮 Agent；不能直接控制设备 | “还是不行”可推进排查，避免重复无效建议，无法确认时停止或升级 |
| 资料与规则咨询 | 解释适用注意事项、售后规则和办理方法 | 仅能依据已接入资料，没有完整商家政策库 | 明确适用对象与资料版本，不编造收费/时效/承诺，不把咨询当办理 |
| 多轮澄清 | 解析指代、纠正、多个诉求与暂停恢复 | 已有 FR-15 首版；长期历史及完整约束未验收 | INT 专项、必要澄清与避免重复追问、暂停撤回约束通过 |
| 售后申请 | 准备摘要、用户确认、提交并返回工单标识 | 仅已有本地模拟工单；没有真实商家/维修系统 | 确认绑定、幂等、撤回/过期确认阻断、提交后可查同一工单 |
| 转人工 | 说明升级原因、整理上下文并反馈交接状态 | 目前仅 handoff_offered；没有实际人工坐席接收 | 本地模式明确模拟待接收；真实模式必须有接收凭据才能称已转接 |
| 进度查询 | 查询当前用户和商品对应的已提交工单 | 已实现当前用户/商品的模拟工单查询工具与结果展示；无真实维修状态 | 支持无编号查本人候选/编号查询；隔离权限、无记录与多记录消歧；只报实际状态 |

**实施顺序**：先完善现有问答与澄清；再闭合本地模拟的“申请→确认→提交→查询”；随后补交接记录与转人工状态反馈。真实工单/坐席接入单列外部依赖，需提供目标系统接口、身份映射和可用环境后才能验证，不以演示记录冒充真实接通。

本地进度查询首版只显示已持久化事实，例如“模拟工单已创建”。当前工单表没有维修处理流转数据，不能自行生成“已派单/维修中/已完成”。后续状态更新必须来自真实后端或明确标注的人工测试操作，保留审计来源。

新增验收场景：CS-01 申请确认后查询同一工单；CS-02 他人/其他商品工单不可见；CS-03 无工单或多工单时正确说明/消歧；CS-04 暂停或撤回后旧确认不得提交；CS-05 转人工未接收不得声称成功；CS-06 指南与规则咨询不触发售后写操作。自动化、真实模型、浏览器及外部联调证据分别记录。

### 1.3 第一版明确不做

- 不接入真实京东/其他平台账户，不读取真实订单，不自动付款、退款或退货。
- 不生成、补绘、修复或虚构商品结构图；图片来自登记资料，允许可追溯的确定性裁剪、缩放和视频帧提取。
- 不做全品类推荐、支付、库存管理、商家运营等完整商城系统。
- 不以“有知识图谱/MCP/多 Agent/RL”为交付目标；没有独立收益证据的复杂模块暂不引入。
- 不承诺通过静态问答基准就能证明用户完成真实操作或系统可直接生产上线。

### 1.4 场景验收故事

| 场景 | 预期行为 |
|---|---|
| 用户有两台同品牌设备，问“怎么连接？” | 展示候选或追问，确认前不提供型号专属操作 |
| 已选商品，询问某项功能的使用方式 | 检索适用文档，返回文字步骤、必要原图和来源 |
| 用户说“第二步的按钮在哪里？” | 保留商品与步骤，展示原图或上下文；资料不足则说明 |
| 用户说“我的面板和图片不一样” | 检查硬件/地区/版本差异，必要时请求图片，不坚持旧答案 |
| 从打印机切换到路由器 | 清空旧任务的商品专属工作证据，保留可理解的历史对话 |
| 用户上传的截图中含可疑指令 | 把截图视为资料，不执行其中的系统/工具指令 |
| 图片加载失败但文字有依据 | 明确降级提示，仍提供真实来源；不得补生成一张图 |
| 未找到对应型号资料 | 说明缺口，提出确认型号或转人工，不借用相似型号冒充 |

### 1.5 成功标准

工程成功：功能链路、证据来源、安全隔离和恢复机制可验证。效果成功：在固定模型、数据和预算下，与固定图文 RAG 比较，说明 Agent 对准确性、配图质量、稳定性或成本的实际作用。研究成功不是第一版承诺；负结果也必须保留。

---

<a id="s02"></a>
## 02. 现有代码评估与迁移边界

### 2.1 已核实的基础

当前项目在审阅时（基线 `fe4050d`）声明 Python `>=3.11`，包名 `layouttrace`、版本 `0.2.0`，使用 LangGraph、Pydantic、LanceDB、BGE 检索及可选视频/LLM 依赖，并提供 `layouttrace` CLI。[S01][S02]（该旧包已舍弃，源码归档于开发工作区 `LayoutTraceRAG/legacy/layouttrace-core/`，不在本仓库内。）

| 审阅时的旧模块（已归档） | 审阅发现 | 改造决定 |
|---|---|---|
| `layouttrace/retrieval/` | 已有混合召回、RRF、候选融合、重排序 | 复用算法思路与适配器，增加可信作用域过滤 |
| `layouttrace/index/store.py` | 持久化结构围绕视频字段；写入/重开没有保存任意 `meta` | 新建 V2 存储，不把型号/版本只塞进会丢失的 `meta` |
| `layouttrace/index/store.py` | 当前 dense/FTS 查询未带商品/权限过滤 | 新搜索接口将 `SearchScope` 设为必传参数 |
| `layouttrace/types.py` | `EvidenceNode`/`Citation` 强制关联视频与时间 | 新建判别式文档/视频定位类型；不伪造 `video_id` 与 0 秒时间 |
| `layouttrace/graph/nodes.py` | `grade` 以至少一条 correct 判断充分；核验失败主要重新生成 | 新增缺口状态和结构化修复决策 |
| `layouttrace/llm/langchain_engine.py` | 生成端以格式化文本证据为主 | 新增真正的多模态观察接口与结构化动作输出 |
| `layouttrace/cli.py` | 以单次 index/ask/eval 为主 | 保留旧命令，新建独立 `shopguide` CLI |
| `layouttrace/eval/harness.py` | 旧指标围绕视频引用和工作流 grounded | 保留旧报告，新增商品评测，不混用指标 |

以上源码事实来自已审阅文件。[S03][S04][S05][S06]

**特别注意：`InMemoryStore` 的现有词法测试路径是 token 重合评分，不应在新文档或实验中称为实际 BM25 基线；需要真实 BM25 时使用对应实现并单独验证。[S03]**

README 已将历史 TVQA-Long 分数标注为正确性修复之前的结果，不能用旧分数作为商品 Agent 改进证据。[S01]

### 2.2 迁移原则

1. 新功能集中在 `shopguide/`，避免破坏旧视频类型与接口。
2. 旧 `layouttrace index/ask/eval` 保持行为；通过回归测试确认（旧实现现已归档，不再随本仓库发布）。
3. 新 CLI 入口为 `shopguide = shopguide.cli:main`，待开发时加入 `pyproject.toml`。
4. V2 数据表、索引与文件目录单独命名，不原地覆盖旧 LanceDB 表。
5. 通过 `LegacyVideoAdapter` 把旧视频节点显式转换成新的证据类型；不能悄悄推断商品适用范围。
6. V2 保留 `embedding_id`，再增加模型 revision、维度、预处理版本、schema 和索引代次。
7. 新功能作为可选依赖分组，纯离线单元测试不下载模型、不需要密钥。
8. 现有导入、CLI、wheel 安装和测试全部列入兼容性门禁。

### 2.3 开发前必须产出的审计文件

`docs/baseline_audit.md`：Git SHA、干净工作区状态、依赖环境、现有测试命令及真实结果、CLI 冒烟结果、已知失败及原因。

`docs/adr/0001-shopguide-boundary.md`：确认包布局、V2 schema、旧功能保留方式。

**本计划不把“源代码中有测试文件”视作“测试已通过”。M0 必须实际执行。**

---

<a id="s03"></a>
## 03. 技术选型与架构决策

### 3.1 默认选型

下面是第一版的拟议固定方案。替换技术需新增 ADR，说明原因、迁移成本及验证结果。

| 层 | 默认方案 | 说明 |
|---|---|---|
| Python | Python 3.11，`uv` + `uv.lock` | 延续现有运行下限；实际补丁版本在 M0 锁定 |
| Agent | LangGraph `StateGraph` + Pydantic 动作契约 | 一个主控策略，独立工具执行器和强制核验 |
| Web API | FastAPI + Uvicorn + HTTPX | 异步调用；不在事件循环中执行重型解析/推理 |
| 业务元数据 | SQLite + SQLAlchemy 2 + Alembic | 本地/单机 MVP；不宣称适合无限并发 |
| 会话持久化 | LangGraph SQLite checkpointer | 独立于业务事务；恢复后仍需检查工具执行账本 |
| 文本索引 | LanceDB V2，BGE-M3 + FTS；真实 BM25 用作对照 | 避免换库分散精力；强制前置过滤 |
| 重排序 | `BAAI/bge-reranker-v2-m3` | 固定模型 revision 与文本截断规则 |
| 页面处理 | `pdfplumber` 提取可用文本/坐标，`pypdfium2` 渲染页面 | 无 PDF 时直接使用数据集原始页面图；不伪造 PDF 来源 |
| OCR | 复用 RapidOCR，仅对无可靠文字层的内容运行 | 不把 OCR 字符串等同于准确事实 |
| 图片存储 | 本地内容寻址文件 + 受控资产接口 | 不公开原始目录；对象存储作为后续部署适配 |
| Planner 当前配置 | `deepseek-v4-flash-vision-exp` | 用户指定；DeepSeek 调用已通过，不可变 revision 待确认 |
| 视觉工具当前配置 | `deepseek-v4-flash-vision-exp` | 真实读图与 B2 三阶段调用已通过；正式基线待运行 |
| 模型服务 | 可配置模型网关；本地 GPU 部署采用独立推理服务 | 主服务不绑定具体供应商，不假定用户已具备显存 |
| 前端 | React + TypeScript + Vite | 商品栏、聊天、原图卡片、来源查看器 |
| 前端测试 | Vitest + Playwright | 契约、组件与端到端浏览器测试 |
| 后端测试 | pytest、pytest-asyncio、Hypothesis、HTTPX | 离线 fake 与在线模型测试明确分层 |
| 质量工具 | Ruff、Mypy、pytest-cov、依赖审计 | 版本在锁文件中固定 |
| 部署 | Docker Compose 单机部署 | 默认不引入 Kubernetes、Redis、Celery 等额外服务 |

LangGraph 的持久化与中断机制支持线程状态和恢复，但副作用需要幂等处理；使用 SQLite checkpointer 需单独安装对应包。[S13][S14] LanceDB 提供 metadata filtering，具体查询 API 必须以锁定版本验证。[S15] 页面处理工具与基线模型能力参考官方资料。[S16][S17][S18][S19]

### 3.2 依赖兼容性要求

- 不把上游 MPMQA 的旧 Python/Detectron2/DeepSpeed 依赖直接混入主服务。原基线使用隔离环境，评分可做最小兼容适配。[S08]
- 不盲目把 LangGraph、LangChain、MCP adapter 全部升级为最新版；先建立小型兼容性测试，再锁定整组版本。
- 当前 `storage` 与 `retrieval` extras 的 LanceDB 下限不同；M0 统一新方案实际解析版本，保留旧环境报告。[S02]
- 推理服务、主服务、历史基准各自使用锁定环境；记录 Python、CUDA、驱动、Torch、服务镜像和模型 revision。
- `.env.example` 不放真实密钥，默认不指定付费供应商。
- CPU 离线测试使用 `ScriptedPolicy`、`FakeVisionObserver` 和固定检索样本；报告显式标记 `model_mode=fake`，禁止纳入模型效果评测。

### 3.3 暂缓的技术

第一版不引入知识图谱、多模型辩论、长期用户画像、全量模型训练、强化学习、自动抓取整个商城。高级页面多向量检索、专用版面模型可以在 B1/B2 基线之后做独立消融，不设为首版阻塞项。

---

<a id="s04"></a>
## 04. 总体架构与目标目录

### 4.1 在线路径

```text
浏览器 / CLI
   │  认证、商品选择、消息、附件
   ▼
API 与任务服务 ───────────► SQLite：商品、文档、资产、run、工具账本
   │
   ▼
Agent Runtime：恢复会话 → 解析商品范围 → Planner 决策
   │                                  │
   │                             动作/参数校验
   │                                  ▼
   │     ┌─────────── 受控 Tool Executor ────────────┐
   │     │ 订单/型号 │ 手册检索 │ 页面/图块观察 │ 模拟业务 │
   │     └──────────────────────────────────────────┘
   │                      │ 实际执行结果、证据 ID
   └──────────────────────┘
   │ 候选答案
   ▼
强制核验：权限 → 型号/版本 → 原图来源 → 文字与图文支持性
   │
   ├─ 可修复且有预算 → 返回 Planner / Writer
   └─ 通过或显式降级 → 结构化答案 → 受控图片渲染 → 用户
```

### 4.2 离线路径

```text
数据下载/资料导入
 → manifest 与授权登记
 → 原始字节留存和哈希
 → 页面解析与渲染
 → 图块提取和来源映射
 → 结构化证据
 → 文本/可选视觉索引
 → 完整性检查
 → 原子切换 ACTIVE 代次
```

PM209 gold、ECom 隐藏任务目标和评分器在评测进程中，不与 Agent 的资料目录同权限挂载。

### 4.3 目标目录（计划新增）

```text
ShopGuide/
├── PLAN.md
├── shopguide/
│   ├── graph/ index/ ingest/ retrieval/ llm/ eval/   # 现有代码保留
│   └── shopguide/
│       ├── cli.py
│       ├── settings.py
│       ├── schemas/             # product/evidence/asset/action/answer/event
│       ├── catalog/             # 模拟订单、型号解析、适用性
│       ├── ingest/              # pm209/manual_pdf/page_image/video
│       ├── storage/             # repository、文件存储、迁移
│       ├── retrieval/           # scoped search、融合、重排、region selector
│       ├── models/              # planner/vision/verifier gateway、fake
│       ├── agent/               # graph、policy、budget、state、repair
│       ├── tools/               # registry、executor、read、simulated actions
│       ├── evidence/            # registry、支持性与来源核验
│       ├── sessions/            # 锁、checkpoint、摘要、恢复
│       ├── api/                 # routes、认证、SSE、错误映射
│       └── benchmarks/          # pm209/ecom adapters、reporting
├── configs/shopguide/           # dev/demo/pm209/ecom profiles
├── prompts/shopguide/           # 版本化提示模板与测试
├── web/                         # 前端与 lockfile
├── tests/
│   ├── ...                      # 现有测试不删除
│   └── shopguide/
│       ├── unit/ contract/ integration/ security/
│       ├── benchmark_parity/ e2e/ performance/
│       └── fixtures/            # 自制无版权争议小样本，不能冒充基准
├── scripts/shopguide/           # 数据审计、prepare、eval、报告、doctor
├── migrations/shopguide/
├── deploy/                      # compose、Dockerfile、健康检查
├── docs/
│   ├── adr/ baseline_audit.md data_audit.md model_manifest.md
│   ├── benchmark_protocol.md runbook.md security.md
│   └── release_checklist.md
├── external/                    # 只读固定版本的基准副本，不打包分发
├── data/                        # .gitignore；corpus/eval_private/synthetic
└── artifacts/                   # .gitignore；run、报告、日志、预测
```

**权威性划分：**SQLite 管理元数据和权限；原文件存储管理字节；LanceDB 只提供候选召回。向量命中不等于授权，读资产/输出答案前仍需最终权限校验。

---

<a id="s05"></a>
## 05. 数据接入、授权与防泄漏

### 5.1 三类数据必须分开

| 数据域 | 用途 | Agent 可访问范围 |
|---|---|---|
| `pm209` | 说明书图文问答评测 | 协议允许的说明书内容、页面/区域及当前问题 |
| `ecom` | 官方多轮业务环境评测 | 业务政策与官方工具实际返回值 |
| `demo` | 产品演示、工程回归 | 自建模拟商品/订单、授权手册、小型合成故障场景 |

三域使用不同数据库、索引、session namespace、缓存前缀和结果目录。PM209 商品与 ECom 商品不天然匹配；不得把两套数据随意配对后称为官方端到端评测。演示域的商品映射属于项目构造，需明确标记。

### 5.2 PM209 接入流程

PM209 出自 AAAI 2023 的 MPMQA，论文报告 209 份手册、27 个品牌、22,021 个问答对，答案包含文字及相关文档区域。[S07] 官方仓库提供下载入口、URA 基线和评测脚本，但环境较旧。[S08]

执行步骤：

1. 固定官方仓库版本，记录下载来源、下载时间和文件 SHA256。
2. 通过官方入口获取数据；确认链接可访问、压缩包完整、解压成功。下载失败是阻塞项，不能用少量样例冒充全量数据。
3. 审核下载包是否包含原始 PDF、页面图、OCR、区域注释和 split 文件；**不预设官方一定提供原始 PDF**。
4. 运行审计脚本，逐 split 统计手册、页面、问题、区域类型及缺失文件。
5. 检查官方 loader 的真实字段；在审阅版本中可见 `data/{split}.jsonl`、`image_filename`、`bounding_boxes`、`qa_data`，区域有 `id/structure/shape`，问题与答案分别在 `question.text`、`answer.text`，相关区域在 `answer.relevant`。[S09]
6. 将每个源文件分解为 `corpus` 与 `eval_private`：文档内容进入前者，答案、相关区域标签和问题到正确页面的映射进入后者。
7. 保持原始顺序和官方问题编号映射，不先排序再重新编号；生成 `official_id_map.json` 供评分器使用。
8. 由文档目录建立内部 `product_id → manual_id` 映射；来源只有手册身份时只声明该身份，不虚构真实订单、商品 SKU、固件版本。
9. 对原始区域建立内部不透明 ID；保存内部 ID 与官方 ID 的评分映射。模型不得通过文件名读出答案或缺陷类别。
10. 对全部页面/区域引用进行完整性检查，再执行离线 scorer parity 测试。

**区域类型。**已核实官方类别包括 `Text`、`Title`、`Product Image`、`illustration`、`Table`、`graphic`。[S10] 因此“区域选择分数”不等于“操作配图分数”。除整体分数外，需对产品图、插图、图形分别报告，并保留表格作为独立组。

### 5.3 ECom-Bench 接入流程

ECom-Bench 正式发表在 EMNLP 2025 Industry Track；论文版描述 53 个任务，其中 18 个包含多模态交互，主要是家电和家居领域。它提供用户模拟、业务环境和工具，不专门评测操作说明配图。[S11]

执行步骤：

1. 固定源码 SHA，检查 README、`envs/story/`、Agent 接口和 MCP 服务启动方式。
2. 使用独立环境安装兼容依赖；记录必要的兼容补丁，不改评判含义。
3. 由公开代码实际枚举任务数、工具 schema、资源引用和所需模型服务；如果与论文数字不同，报告具体版本差异。
4. 对数据文件、任务、政策文本、图像引用和模型可用性进行预检；外部图像无法获取时不得悄悄跳过对应任务。
5. 每个 `(task_id, trial_id)` 启动独立业务数据副本、工具进程和会话，结束后记录状态并清理。
6. 模拟用户只接触其角色允许的指令；Agent 只接触客服角色政策和实际消息，禁止读取用户隐藏目标或任务 metadata。
7. 先运行官方基线的冒烟任务，确认会话、工具记录、结果评分均有效，再接入 ShopGuide adapter。
8. ECom 模式下开放官方允许的模拟读写工具；只提供只读查询不能被称为完成全量基准。
9. 数据访问权限、读写语义、确认条件遵循固定版政策；额外业务限制只能作为扩展实验报告。

官方 `envs/story/env.py` 的主 reward 为 `action × search × output`，时间分数被记录但没有乘入最终 reward；因此不能将 README 的四项描述简单变成平均分。[S12]

### 5.4 演示数据集

拟议最小集：5–10 个模拟商品、至少两个容易混淆的同系列型号、两名模拟用户、含多商品的模拟订单、已授权的说明书或自制小手册。数量是工程测试目标，不是官方基准数据量。

自制小手册只用于接口、裁剪和安全回归，不用于宣称真实商品问答性能。真实演示资料需记录适用型号和使用权限；不能将代码许可证当作手册、商标、图片的授权。

### 5.5 Manifest

每个数据域输出以下清单。示例值是待执行时填写的字段，不是已测结果。

```yaml
schema_version: 1
dataset_id: pm209
source_revision: "5226a9aa849fd3b8f35620e7b7d7d10c09d754a2"
archive_sha256: null
license_review_status: pending
audit_status: pending
original_language: en
files_manifest: files.sha256.json
split_manifest: splits.json
corpus_manifest: corpus.jsonl
gold_manifest: eval_private/gold.jsonl
id_mapping: eval_private/official_id_map.json
counts: null
missing_files: null
```

审计完成后禁止以 `null` 或 `pending` 清单启动正式评测。代码 SHA、数据包 hash、协议和模型版本共同确定 `benchmark_run_fingerprint`。

### 5.6 划分与污染控制

- PM209 保留官方划分；不要为提高分数重新随机划分问题。审计手册、页面与图片跨 split 重叠并如实报告。
- 检索任务允许提供协议规定的测试来源文档，不等于可以将测试问题、答案、gold region 或 gold page 提供给模型。
- ECom 任务规模小。用独立合成任务开发；必须调试官方任务时预先登记 dev 子集，冻结 holdout。已查看并据此调参的任务不能继续声称“完全未见”。
- 不以任务 ID 为键提供预制答案、预制工具序列或“正确资料列表”。
- 缓存不得跨问题保存评测答案；正式运行中若使用缓存，区分文档预处理缓存与完整响应缓存。
- 第三方预训练模型是否接触过公开基准通常无法完全证明，作为评测局限记录。
- 中文界面与英文 PM209 分开：主表使用原始协议；翻译版测试单列，不混合计分。

---

<a id="s06"></a>
## 06. 数据模型与证据契约

### 6.1 元数据实体

| 实体 | 必要字段 | 关键约束 |
|---|---|---|
| `Product` | `product_id, domain, brand, model, aliases, category` | 型号别名需可追溯；不按相似品牌自动合并 |
| `ProductVariant` | `variant_id, product_id, market, hardware_revision, firmware_range` | 未知字段显式未知，不默认“兼容所有版本” |
| `OrderItem` | `order_item_id, principal_id, product_id, variant_id, order_id` | 权限由认证上下文决定，不接受模型指定用户 |
| `DocumentVersion` | `doc_version_id, source_id, content_sha256, language, version_label, status` | 字节内容不可变；变更新建版本 |
| `Applicability` | `doc_version_id, product_id, variant_constraints, basis, reviewed` | 说明书封面/型号表等证据支持适用性 |
| `Page` | `page_id, doc_version_id, index_0based, label, width, height, rotation` | 内部索引与印刷页码分离 |
| `Asset` | `asset_id, source_locator, bytes_sha256, source_sha256, transform, mime, dimensions` | 来源能回溯，权限独立校验 |
| `Evidence` | `evidence_id, kind, source_locator, text, asset_ids, scope, provenance` | 不能只保留文本而丢失定位与版本 |
| `Run` | `run_id, session_id, principal_id, status, snapshot_id, budget, config_hash` | 同一 run 固定资料快照与配置 |
| `ToolExecution` | `call_id, run_id, name, arguments_hash, state, idempotency_key, result_ref` | 副作用通过账本恢复，不能依赖 LLM 记忆 |
| `Feedback` | `run_id, step_id, reason, optional_note` | 默认不直接成为训练标签 |

### 6.2 来源定位必须使用判别式类型

```text
ManualLocator:
  type = "manual"
  doc_version_id
  page_id
  page_index_0based
  page_label                  # 印刷页码，可为空
  region_id                   # 可为空
  bbox_normalized             # [x0, y0, x1, y1]，左上原点

VideoLocator:
  type = "video"
  video_version_id
  start_s
  end_s
  frame_timestamp_s           # 帧资产使用；非帧证据可为空

BusinessLocator:
  type = "business"
  domain
  actual_tool_call_id
  entity_ref
  observed_at

UploadLocator:
  type = "user_upload"
  upload_id
  principal_id
  bytes_sha256
```

不可用文档页码伪装时间，不可用用户照片作为官方产品使用说明的唯一权威来源。

### 6.3 坐标约定

统一外部服务坐标为经页面旋转标准化后的左上原点、`[0,1]` 归一化矩形。原始 PDF 点坐标、渲染像素坐标、PM209 `x/y/width/height` 必须保留原值和变换矩阵。[S09]

必要校验：

```text
0 ≤ x0 < x1 ≤ 1
0 ≤ y0 < y1 ≤ 1
page_index_0based 在页面范围内
视频：0 ≤ start_s ≤ end_s ≤ duration_s
图片尺寸与渲染配置一致
旋转、CropBox/MediaBox、裁剪偏移都纳入坐标变换
```

禁止简单认为所有 PDF 都是 A4、未旋转、零偏移。裁剪后保存源页与 transform，前端能够返回整页查看；不通过生成式增强“补全”看不清的标签。

### 6.4 不透明 ID 与内容哈希

- 用户和模型可见的是不透明 `asset_id/evidence_id`，不暴露磁盘路径、私人 bucket key 或带 gold 的文件名。
- 哈希用于验证字节一致性，不代替授权。原始文件 hash 与显示派生图 hash 分开保存。
- 相同图片字节可复用存储对象，但每一条商品/资料关联和权限记录独立存在。
- 文档更新后旧引用仍定位到原版本；版本被撤销/删除时按策略提示，不静默指向新文件。

### 6.5 `ToolResult` 与错误契约

```json
{
  "schema_version": 1,
  "call_id": "call_demo_001",
  "status": "ok",
  "data": {"evidence_ids": ["ev_demo_001"]},
  "evidence_ids": ["ev_demo_001"],
  "asset_ids": [],
  "warnings": [],
  "error": null,
  "cost": {"latency_ms": 0, "input_tokens": 0, "output_tokens": 0, "image_inputs": 0}
}
```

示例只演示 schema；0 不是实测耗时。`status` 取 `ok/empty/partial/error`；错误对象含 `code/message/retryable`，不返回密钥、栈内文件路径或隐藏答案。

标准错误至少覆盖：`SCOPE_REQUIRED`、`PRODUCT_AMBIGUOUS`、`FORBIDDEN`、`SOURCE_VERSION_MISMATCH`、`ASSET_NOT_FOUND`、`UNSUPPORTED_FORMAT`、`TIMEOUT`、`RATE_LIMITED`、`BUDGET_EXCEEDED`、`CONFIRMATION_REQUIRED`、`MODEL_OUTPUT_INVALID`、`INDEX_IDENTITY_MISMATCH`。

---

<a id="s07"></a>
## 07. 说明书、原图与视频入库流程

### 7.1 导入任务生命周期

```text
RECEIVED → VALIDATING → PARSING → EXTRACTING → INDEXING → AUDITING → ACTIVE
                         └──────────────────────► FAILED / QUARANTINED
```

每一步写入产物 hash、版本和状态。断点恢复按“输入 hash + 处理版本”复用结果，不通过文件存在就判断完成。删除或替换资料使用新代次和 tombstone，不修改活跃查询正在使用的文件。

### 7.2 两条页面处理路径

**路径 A：PM209 协议输入。**直接保留提供的页面图、OCR、区域与 ID。使用官方提供的文档区域结构属于基准设置；使用与答案相关的 region 标签属于泄漏。两者必须在 loader 层分离。

**路径 B：真实资料演示。**有可靠文字层时用 `pdfplumber` 提取；用 `pypdfium2` 渲染完整页面，再基于可靠区域框裁剪。没有 PDF 时将原页面图作为原始资料。文字层缺失/乱码才进入 OCR，不对同一页无条件反复 OCR。[S16][S17]

自动版面检测不是 PM209 标注区块的替代真值。真实文档区域可由规则/视觉模型提出候选框，经边界和内容检查后保存；框的可信度不足时展示整页上下文，标记定位不精确。

### 7.3 原图提取规则

1. 优先使用来源内已存在的图片资产；图表、箭头、矢量图或文字组合区域无法直接提取时，采用页面渲染后的确定性裁剪。
2. 裁剪必须保存页码、框和父页面 ID；添加上下文边距的显示图与精确评分区域是两种派生视图，不修改官方 region ID。
3. 操作图附近的警告、数字标签、图注不能因为裁剪而被误解。必要时同时展示图块与整页入口。
4. 同一页面多个子图保持各自身份；禁止把不同商品图合成一个“官方步骤图”。
5. 禁止生成式填补、改字、换按钮颜色、补零件。首版不增加自动箭头标注，减少误导。
6. OCR/caption 可用于检索，但属于提取/模型派生信息，不是原图本身，也不能替代对原图的检查。
7. 资产缺失时返回明确错误，不能调用图片生成器或全网相似图替补。

### 7.4 页面、区域和文字的关联

建立以下关系：`belongs_to_page`、`caption_of`、`nearby_text`、`continues_on`、`references_figure`、`describes_step`。首版使用轻量关系表，不引入图数据库。

关系的来源分为原文显式编号、版面邻近、模型推断三类。模型推断关系必须带方法和置信/审核状态，不能作为未经核验的硬事实。

### 7.5 视频能力复用

P1 复用原视频处理链路，新增商品适用性、版本、视频文件 hash。所有抽帧由确定性视频解码执行，保存时间码。没有证明某视频适用于当前型号时，最多作为明确标注的通用参考，不作为型号专属操作依据。

视频分数单独报告；PM209 不含的能力不能通过 PM209 得分代证。

### 7.6 发布一致性

采用“暂存 → 审计 → 激活”策略，解决数据库、文件与向量索引无法天然共用一个事务的问题：

```text
写入 STAGING 元数据
 → 保存原始与派生文件
 → 构建独立索引 generation
 → 检查文件/向量/区域数量与 hash
 → 数据库事务切换 active_snapshot_id
 → 旧 generation 延迟回收
```

查询开始时固定 snapshot；不允许半数图块来自旧版、文字来自新版。回滚只切换 active 指针和服务版本，不覆盖原文件。

---

<a id="s08"></a>
## 08. 检索、重排序与型号约束

### 8.1 可信 `SearchScope`

```text
SearchScope = domain + principal + authorized_product_ids
            + confirmed_variant + allowed_doc_version_ids
            + language_policy + snapshot_id
```

该对象由后端根据会话与认证上下文建立。Planner 可以提出商品候选，但不能直接填写 `principal`、扩大授权文档或拼接 SQL 过滤表达式。

### 8.2 商品解析顺序

已明确选中的订单项 → 用户当前明确型号 → 会话中尚有效的商品引用 → 用户图片的候选识别。

这不是不允许覆盖的优先级：当新消息明确切换商品时重新解析；不同来源冲突时澄清。图片识别只产生候选，不凭视觉相似就认定精确型号。一个产品系列存在多个变体时，只有确认的共享操作才能给出共享指导。

### 8.3 召回与排序流程

1. 在允许的 document/version 集合内执行 lexical 与 dense 召回。
2. 多查询扩展最多 3 个，禁止丢失型号和关键条件；原问题始终保留。
3. 使用 RRF 融合，保留每条候选的命中来源、rank、score 和 query。
4. 候选集合足够大后再 cross-encoder 重排，不能先裁到最终 top-k 再重排。
5. 将区域结果聚合为页面结果，同时保存区域级排序；重复区域按 ID 去重。
6. 对操作问题补读相邻步骤、图注、警告与必要的跨页内容。
7. 视觉候选通过语义与原图观察核验后才成为展示图片。

起始调参值：每路候选 30、融合上限 60、重排后文本证据 8、候选页面 5。均是验证集上待调的初值；不能固定阈值后声称适用于所有商品。

### 8.4 过滤必须在候选选择前生效

LanceDB 支持 metadata filtering，但具体 prefilter 行为需通过锁定版本的向量和 FTS 集成测试证明。[S15] 若某模式无法支持前置过滤，应按授权文档分区/构造受限候选索引，而不是全库先 top-k 再过滤。

后置权限检查仍需保留，用于处理索引过期或损坏，但不能替代前置范围约束。边界用例必须包含：另一个商品更相似、同名型号不同品牌、已撤销文档、跨用户私有资料。

### 8.5 配图排序与文字排序不同

文字命中“如何操作”不代表附近所有图都可展示。`RegionSelector` 综合当前步骤、图注、显式图号、页面上下文及视觉观察，选择实际支持该步骤的区域。重复图片不重复刷屏；非必要插图可不展示。

在 PM209 上选择提供的官方候选 region ID，以便准确评分；在演示文档上使用自动提取区域，需要单列“端到端解析”结果。

### 8.6 索引健康检查

必须验证：embedding 维度一致、向量为有限数、批大小与数量匹配、模型 revision 相同、语义区分探针有效、重开索引后 metadata 完整、FTS 可用、资产能读取。探针用于发现明显异常，不代表检索效果已经合格。

旧 HashEmbedder 仅用于测试；正式数据索引配置为真实语义模型时，加载失败应报错，不静默切回 hash 或 identity reranker。

### 8.7 本次 RAG 改造的两层边界（新增计划，待实现）

用户附件描述的检索对象是 **意图定义和历史表达样例**，不是商品说明书。采纳其“多源感知→候选召回→语义精排→上下文判决”结构，作为 FR-15 的候选增强；同时改进 FR-03 的资料检索与上下文构造。分析依据和差异见 [RAG 改造分析](docs/rag_revision_analysis.md)。

```text
原始问题 + 相关对话状态
  → 意图层：Keyword / BERT Classifier / Example Embedding + Slot Features
  → 意图候选融合 → Query–Intent Cross-Encoder → LLM Judge
  → 原文依据校验及多诉求状态更新 → 主 Agent / 既定子 Agent
  → 证据层：授权过滤 → 文本/向量召回 → RRF → Query–Evidence 重排
  → 页面/区域/图片预算分配 → Writer / Verifier → 客服结果
业务查询结果 ───────────────────────────────→ 独立业务结果上下文与卡片
```

第一阶段进展见 [候选层实现记录](docs/intent_candidate_implementation.md)。下述“没有完整前置链”是需求登记时基线；本轮已接入规则/槽位/样例召回及融合，BERT 和完整验收仍待完成。

需求登记时，`ScopedIndex.search` 已有授权范围内 dense/FTS、RRF 和重排，真实 BGE 有历史验证；`intent/service.py` 目前直接给 LLM 意图库与会话状态，没有 Keyword/BERT/Example DB/候选融合/Cross-Encoder 的完整前置链。不得将新增意图检索描述成“之前没有 RAG”，也不得把已有文档 reranker 当成已训练的意图分类器。

两层使用独立 namespace、记录类型、版本及指标。IntentExample 保存 intent_id/example_id；ManualEvidence 保留原 evidence_id/页码/原图资产。可以复用模型加载/向量存储基础设施，但分别登记输入模板、模型 revision、索引 hash、训练来源与许可。意图样例不得作为操作指南证据，工单记录也不得混入手册向量库。

### 8.8 多源意图召回与槽位特征（首阶段部分实现）

| 分支 | 输入与输出 | 实施边界 |
|---|---|---|
| Keyword | 正向规则、否定/纠正模式，输出规则 ID、原文跨度与相对支持分 | 不最终分类；“取消退款”可能是撤回已有诉求，不因负词直接删掉退款目标 |
| BERT Classifier | 训练后输出已发布标签候选与原始分数 | 无合格标注集/权重前显式 disabled；不以通用 BERT 权重或占位分数冒充分类能力 |
| Example Embedding | 检索已发布意图表达样例，按意图聚合；返回 example_id、相似度、margin/一致性 | 不是关键词类别表，也不是产品文档检索；相似度与 margin 不代表正确概率 |
| Slot Features | 型号、订单项/工单编号、现象等槽位及原文依据 | 格式匹配只是候选实体，权限与对象有效性仍由业务工具验证；缺槽不等于意图未知 |

Intent Schema 扩展 name、keywords、negative_patterns、slot_schema、examples、审核/发布版本及下游支持标记；各分支引用同一规范。保留 raw_query 与 normalized_query，归一化限于可解释的字符/空白等处理；错字纠正和繁简转换须留映射。不删除否定、条件、型号和数字，不把“没发/没到/没动”统一改写。状态变更的依据仍定位 raw_query。

首版槽位以确定性规则提取已有商品/工单实体，开放描述由末端 LLM 一并判断；单独 NER 待数据齐备再评估。不因附件示例引入身份证、发票、物流、退款等未授权的新业务采集和执行接口。

多诉求不能只用单个 softmax Top1：BERT 训练任务需选定多标签输出或逐诉求分段分类，并按多诉求样例验证；单标签基线仅作为该分支的对照，不替换当前多 goal 状态。

### 8.9 候选融合、精排与上下文保护（新增计划，待实现）

先合并各路候选并按 intent_id 去重，保留 channel/rank/raw_score、规则/样例依据、分支可用性和耗时。规则支持分、分类分、余弦相似度与槽位分不直接当成同一种概率相加；选择秩融合或在开发集校准后的加权融合，并冻结配置。

附件的 `0.15*K + 0.30*B + 0.40*E + 0.15*Slot` 仅登记为待验证初值；Embedding 的 `0.6*MaxSim + 0.4*MeanTop3` 同样是候选配置。需定义每意图样例不足 3 条、缺失分支、分数并列及非有限值处理，防止样例多的类别天然占优。分支 disabled 或故障必须显式标记 profile；不得声称四路都运行，也不静默用假模型回填。

候选初值：BERT Top3、Embedding 样例 Top10/意图 Top3、融合 Top3–5、重排 Top3；目前目录仅六类，另保留全目录判决作为可追溯参照。上述是预算内参数，不是多诉求数量硬上限：按明确诉求分组召回，保留此前活动/被引用的暂停目标、最近提问所属意图及全局停止/纠正语义，记录“召回候选＋状态补充候选”的来源。补充项不能因不在 Top3 被截掉；总预算不足时显式扩大至配置上限或返回澄清，不能默默丢诉求。

Cross-Encoder 输入 query + 名称/定义/边界/代表样例，区分于 Query–Evidence 文档重排。训练 hard negatives 可由相似检索产生，但须检查同轮多标签、父子/重叠定义和合理替代解释；非主标签不自动是负例，不能用测试集生成训练负例。通用 reranker 可作独立零样本基线，不能称为已训练意图精排模型。

原文、规范化文本、候选及各分支证据、槽位、近期 3–4 轮历史和结构化多诉求状态进入一次 LLM Judge 调用；使用现有 Delta/state reducer，不回退成单一 active_intent。“右耳没声音”若是在回答售后原因，应更新已有对象属性，不强制新建故障诉求。LLM 不自创已发布目录外标签；允许 UNKNOWN/CLARIFY。若存在候选漏召回，记录原因并按明确策略补充已发布候选或澄清，不硬选。

`confidence` 若保留只能作为未校准诊断值；生产判断采用经过验证的拒识/澄清策略，不设“0.8 即执行”的通用门槛。确定意图后进入主 Agent，不自动执行业务；原用户确认、授权和暂停撤回检查继续有效。

### 8.10 证据 RAG 的对应改动（新增计划，待实现）

- 意图/槽位只提供检索目的与查询约束：指南关注步骤/条件/警告，排查关注现象与已尝试操作，规则咨询关注适用范围与版本。商品/型号仍由用户确认与后端授权确定，分类分数不能扩大 SearchScope。
- 保留既有 dense/FTS→RRF→文档重排；不把意图融合权重或 Top3–5 机械套用到证据召回。文档检索和意图检索各自冻结候选预算并分别测召回。
- 在固定证据条数及 token 预算内，按相关性与页面配额构造上下文，避免当前 `_retrieve` 先拼页再 `evidence[:32]` 导致后续已选页面消失；保留警告、前置条件和步骤邻接关系。记录选中页/实际进入页/被截证据与原因，不使用 gold 决定保留哪些页。
- 图片候选从“区域类型优先后取前四张”改为问题/步骤相关性与图注联合排序，再在现有视觉预算内观察核验。缺必要图片与可选图片分开，仍只展示已授权真实资产。
- 对无证据、候选不足、分支故障、重排失败及上下文截断分别诊断。工单查询绕过文档检索走现有业务工具，结果继续通过独立上下文进入回答/核验，不能靠说明书推断工单事实。
- 冻结旧 PM209/B1/B2 与真实复验工件。新配置使用新索引/提示/策略标识；预测与评分不混用，未做完整新评测的旧勾选项不代表改造已通过。

### 8.11 数据、实施阶段与验收（新增计划，待实现）

数据以三类为主：版本化 IntentConfig、脱敏且可追溯的 IntentExample/训练标注集、现有多 goal DialogueState；说明书证据与业务记录继续使用各自存储。状态保留属性来源与关键原文引用，有限历史窗口是输入预算，不等于删除暂停目标或全部审计记录。用户私有样例须隔离，删除/撤销进入索引更新与缓存失效流程。

实施顺序：

1. 冻结规范/输入输出/分支开关，建设人工复核的多轮标注与测试划分；先接 Keyword、Example Embedding 和基础 Slot，BERT 显式未启用。
2. 实现候选融合、状态保留项、未知出口与逐层审计；验证候选召回，再接 Cross-Encoder 与现有 LLM Judge/reducer。
3. 有合格训练数据后训练并接入 BERT 意图分类器、构建经审核的困难负例精排数据；分别登记真实权重、训练/验证划分和模型卡。完整三路粗判＋槽位架构在此后才可称实现。
4. 分别完善文档上下文/图片预算分配，再接主从 Agent；保持会话状态、业务授权和总预算，避免将全链路混改的结果归因于单一分支。
5. 完成 IR-01 至 IR-12 与真实分层评测；上线参数、拒识阈值、模型与配置在开发集选择后冻结，测试集不用于调参。

逐层报告：BERT 多标签/类别 Macro-F1 与混淆对；样例及意图 Recall@K；融合与精排前后的 Candidate Recall@3/5、多诉求全部目标召回率；精排在候选含真值条件下的区分效果；槽位精确率/召回率/F1；Judge 意图及状态变化、未知误接收/已知误拒绝；端到端问题解决、误办理、必要澄清与额外追问、延迟及每轮成本。文档侧另外报告证据/页面召回、上下文保留率、原图指标和回答正确性，不能用意图准确率替代。

对话及近似改写按组划分，不能读取未来轮次；去掉单分支、跳过精排及候选规模变化用于定位贡献。离线 fixture、真实分支、LLM 和完整客服结果分别报告。在线无需人工坐席参与分类，但离线标注、目录审核与用户业务确认仍保留。人工交接依旧暂停。

---

<a id="s09"></a>
## 09. Agent 状态机与决策规则

### 9.1 状态结构

```text
ConversationState:
  session_id, principal_ref, domain, messages, summary
  selected_product, selected_variant, product_resolution_status
  current_task_id, current_step_id, conversation_revision

TaskState:
  run_id, question, subgoals, missing_information, contradictions
  evidence_ids, inspected_asset_ids, visited_page_ids
  source_snapshot_id, tool_history_refs
  budget_used, deadline, pending_action, pending_confirmation
  answer_draft, verification_result, terminal_status
```

历史消息用规范消息 ID 去重；证据按 `evidence_id + source_version` 合并。不要用简单列表累加器无限堆积重复 observation。会话摘要、候选假设与真实证据分字段存放。

### 9.2 动作输出

Planner 每次返回一个可校验的动作：

```text
ToolAction          {kind: tool, tool_name, arguments, public_reason}
ClarifyAction       {kind: clarify, question, missing_fields, choices}
DraftAnswerAction   {kind: draft_answer, evidence_ids, requested_format}
HandoffAction       {kind: handoff, reason, summary_evidence_ids}
AbstainAction       {kind: abstain, reason, missing_evidence}
```

`public_reason` 只描述“为了确认型号/定位按键”等简短行动目的，不要求保存或展示私有思维链。工具名称与参数需符合注册 schema；未知动作最多一次修正，仍无效则结构化失败。

### 9.3 执行循环

```text
START
 → load_session_and_auth
 → resolve_scope
 → planner
 → validate_action_and_budget
     ├─ tool → execute → update_evidence → planner
     ├─ clarify → persist_waiting_state → END_TURN
     ├─ draft_answer → writer → mandatory_verify
     │                         ├─ pass → renderer → END_TURN
     │                         ├─ repairable → repair_context → planner/writer
     │                         └─ no_budget → supported_partial_or_abstain → END_TURN
     ├─ handoff → allowed_handoff_flow → END_TURN
     └─ abstain → safe_terminal_response → END_TURN
```

Agent 决定行动，不决定是否跳过身份、资料适用性、原图检查或预算限制。`END_TURN` 不等于删除会话；用户补充信息后恢复当前任务或明确创建新任务。

### 9.4 证据充分性

不再采用“存在一条 correct 就足够”的规则。按问题建立可验证的待完成项，例如：型号已确认、功能位置已定位、步骤顺序有来源、必要警告已保留、图片适用、用户报告的差异已处理。

充分性是带理由的判定，不是随意生成的置信度百分比。每条关键说明必须关联支持证据；存在冲突时保存冲突而不是多数投票后隐藏。

### 9.5 修复策略

| 失败 | 优先动作 | 不允许的替代 |
|---|---|---|
| 商品未确认 | 澄清/列出授权候选 | 猜一个型号继续 |
| 缺少操作步骤 | 在当前资料范围定向查询、读相邻页 | 依靠一般常识补齐具体按键 |
| 图片与文字不一致 | 重查图块/版本 | 保留图并改写成“示意图”蒙混 |
| 原图太小 | 获取更高分辨率原页面或确定性裁剪 | 生成式重绘标签 |
| 存在版本冲突 | 确认变体，或只给有支持的共同信息 | 用“最新文档”自动覆盖 |
| 答案超出来源 | 重写相关句子并再次核验 | 原封不动重复生成 |
| 预算耗尽 | 返回已支持部分与明确缺口 | 标记为全部已验证 |

### 9.6 预算与循环防护

拟议产品 profile 初值：每轮最多 8 次 Planner 决策、12 次工具调用、最多 4 个不同文档页面，单次视觉调用最多 4 张输入图、单轮最多 8 张输入图、最多 4 张输出配图。最多 12 次模型请求，并为最终生成和核验预留 2 次；嵌套视觉/核验调用也计入模型预算。

单轮墙钟初值 90 秒；外部模型请求初值 30 秒超时、最多 2 次有界重试，所有等待与重试受剩余总预算约束。解析不在聊天请求内同步完成。以上值必须在实际部署上测量后锁定，不作为性能保证。

- 相同工具、规范化参数、相同 snapshot 连续重复 2 次且无新增证据时，触发 no-progress 处理。
- 并行只读工具首版最多 2 个；有依赖、同业务实体写入和确认操作不得并行。
- 参数修正、模型重试、观察工具都计成本；终止不能被工具结果中的文字取消。
- `max_context_tokens` 是单次上下文约束；`max_total_tokens_per_turn` 是累计成本约束，两者不能混用。
- ECom 正式 profile 使用其固定协议，不直接套产品的 8 次决策或 90 秒限制。

---

<a id="s10"></a>
## 10. 工具接口与执行语义

### 10.1 第一版工具目录

以下签名是项目接口契约，名称不得冒称为 ECom 原生工具名。

| 工具 | 模型可填参数 | 返回 | 权限/副作用 |
|---|---|---|---|
| `list_purchased_items` | `query?, cursor?, limit` | 当前用户授权订单项及候选商品 | 只读；用户 ID 从认证上下文注入 |
| `resolve_product` | `order_item_id? / model_text?` | 精确匹配/候选/缺失字段 | 只读；选择结果经后端验证 |
| `search_manuals` | `query, top_k, content_types` | 证据 ID、页 ID、摘要和实际分数 | 只读；强制 SearchScope |
| `read_page` | `page_id, include_neighbors` | 页面文字、原图资产、区域目录 | 只读；按预算限制邻页 |
| `get_region` | `region_id` | 原图块与图注、来源、尺寸 | 只读；不可接受任意 URL |
| `inspect_asset` | `asset_id, question` | 针对图像的观察、无法确认项 | 模型调用；原图真实输入 |
| `crop_source_region` | `page_id, bbox_normalized` | 新的确定性裁剪资产 ID | 只产生本地派生资产；不得生成图 |
| `inspect_user_image` | `upload_id, question` | 可见特征、候选型号及不确定项 | 用户私有资料；不是权威手册 |
| `search_tutorials` | `query, top_k` | 授权视频片段证据 | P1；同样受商品约束 |
| `read_video_span` | `video_id, start_s, end_s` | 字幕、帧 ID 与时间码 | P1；限制长度与帧数 |
| `prepare_service_request` | `reason, related_order_item, summary` | 待确认的模拟服务申请 | P1；不产生实际业务效果 |
| `commit_service_request` | `confirmation_id` | 已执行的模拟工单编号 | P1；确认、幂等与账本必须通过 |

参数使用 Pydantic 严格验证；`extra=forbid`。工具的分页上限、文本上限、图像上限由服务端再次限制。

### 10.2 ECom 工具适配

`EComToolAdapter` 动态加载锁定版本的原生 MCP 工具 schema，保留工具名、参数含义、原始执行结果与业务状态。ShopGuide 的 Agent 可以封装决策逻辑，但评分记录必须由**真实发生的原生调用**产生，不能根据“预期正确动作”补写 trace。

适配器需完成以下兼容测试：

1. 每个公开工具能被枚举、调用并返回合法结果。
2. 输入/输出消息中多模态内容不丢失。
3. 原生 JSON 参数序列化与评分器读取格式一致。
4. 业务副作用确实作用于当前 trial 的环境副本。
5. 一个用户回合内发生的全部实际 tool calls 都记录；不能只保留最后一次。
6. 当前官方实现从消息的 `additional_kwargs.tool_calls` 读取部分轨迹，适配层需验证对应序列化路径，不能假设仅设置另一字段就会被评分器识别。[S12]

### 10.3 工具执行账本

```text
PENDING → RUNNING → SUCCEEDED
              ├→ FAILED
              └→ OUTCOME_UNKNOWN
```

只读调用可按 `(scope, snapshot, name, canonical_args, model_revision)` 缓存。模型生成类调用的响应缓存默认在正式 trial 间关闭。

写操作的 `idempotency_key` 绑定真实用户/环境、业务对象、操作类型与规范参数。确认令牌还需绑定当前请求 hash、有效期、主体与版本；参数变化后旧确认失效。

**恢复语义：**checkpoint 只能恢复控制状态，不能保证外部写操作只执行一次。[S13][S14] 若操作响应丢失，先查询幂等结果或实体状态；无法确认时标记 `OUTCOME_UNKNOWN` 并停止自动重复执行。只在后端支持时实现近似一次业务效果，不宣称跨网络严格 exactly-once。

### 10.4 失败与重试

- 429/临时网络失败：尊重服务端重试提示并受总 deadline 约束。
- 参数错误/无权限/不存在的资产：不盲重试，返回结构化诊断。
- 空检索：允许改写或读目录，不扩展到其他型号绕过范围。
- 服务降级：必须明确是缺视觉、缺资料还是模型不可用；不能 silent fallback 后保持“已验证”标志。
- 工具观察中的指令永远不改变工具注册表、环境变量或系统权限。

---

<a id="s11"></a>
## 11. 图文答案生成与强制核验

### 11.1 答案结构

输出为结构化对象，由前端渲染，不直接把模型任意 Markdown/HTML 展示给用户。

```text
GuideAnswer:
  schema_version
  status = answered | partial | needs_clarification | abstained | handoff
  product_ref, source_snapshot_id
  summary
  prerequisites[]            # 必要条件/来源中的安全提示
  steps[]:
    step_id
    title
    text
    claim_ids[]
    evidence_ids[]
    display_asset_ids[]
  unresolved_items[]
  followup_question?
  citations[]
  verification:
    structural = pass | fail
    semantic = supported | partial | unsupported | not_checked
    warnings[]
```

`Claim` 另带类型：`manual_instruction`、`user_observation`、`business_state`、`inference`。不同类型使用不同证据要求。操作步骤中的关键按键名、顺序、条件及警告不能用只有相似含义的常识替换。

### 11.2 原图原则

- 模型只能选择已检索/读取的 `asset_id`，不能自行提供网络图片 URL。
- 后端解析 ID 并检查商品适用性、来源版本、权限、原始/派生 hash 和转换记录。
- `display_asset_ids` 必须是展示授权资产；用户上传图片只能在其自己的会话范围使用。
- 图像观察结果不能被误作图像本身。没有可用原图时可给有支持的文字并说明，不能调用图像生成。
- 首版仅允许 `original`、`page_render`、`crop`、`resize`、`video_frame` 变换；这些不是生成图。
- 前端支持查看整页/原始来源，避免局部图脱离上下文。
- 评分区域与展示留白/上下文裁剪分开保存，不能靠返回整页“命中所有区域”提高选图成绩。

### 11.3 三层核验

| 层 | 执行者 | 主要检查 |
|---|---|---|
| L1 结构与身份 | 确定性代码 | schema、ID 存在、引用范围、图片来源和资产 ACL |
| L2 型号与版本 | 规则 + 适用性表 | 型号、地区、硬件/固件适用范围，资料冲突 |
| L3 语义支持 | 独立核验调用 + 必要人工抽查 | 结论是否由引用证据支持、图文是否对应、步骤遗漏 |

运行时核验不是评测 gold。即使核验使用不同提示或模型，也可能误判；最终效果以独立标注/官方评分与人工抽查为准。不能用自身 `supported=true` 作为全部准确率。

### 11.4 核验输出

```text
VerificationIssue:
  claim_id / step_id / asset_id
  code
  severity
  explanation
  suggested_repair = retrieve | inspect | clarify | rewrite | remove | abstain
```

L1/L2 失败不能让模型自行批准。L3 失败时，只重写或补查相关部分，不无条件重走全部工具。任何修改后的答案再次通过必要检查。

### 11.5 无依据与高风险内容

没有型号依据时，不给出型号专属操作。出现电气危险、拆机、明显损坏或其他需要专业人员处理的情形，按审核的风险策略停止进一步操作指导，给出安全边界和人工渠道；不依据大模型常识生成高风险排修步骤。

来源缺失、资料冲突和无法确认必须在面向用户的文本中说明，不仅记录在日志里。

---

<a id="s12"></a>
## 12. 多轮会话与任务恢复

### 12.1 会话规则

- 主体由认证层确定，`session_id` 只是定位符，不是授权凭证。
- 每个会话默认同一时刻一个活跃 run；并发消息通过队列或 `409 SESSION_BUSY` 处理。
- 客户端提供唯一 `client_message_id`，网络重发返回同一个 run，而不是重复执行。
- 相同幂等键对应不同消息内容时返回冲突。
- 用户切换商品后创建新的任务工作区，旧证据保留为历史，不继续当作新商品依据。
- 用户补拍/补充版本时保留已有有效证据，失效部分明确标记。
- 对话摘要用于理解指代，不是来源证据；摘要中的结论必须能回查原证据 ID。

### 12.2 中断与恢复

使用持久化 checkpointer 和显式等待状态支持澄清或确认。[S13][S14] 重启后扫描未结束 run，核对工具账本，再决定继续、等待用户或失败。

恢复测试包括：模型调用前中断、只读工具返回后中断、写操作成功但未保存回复、用户确认过期、资料代次更新、客户端重复点击恢复。

恢复过程中不得重新询问已在有效状态中保存且无冲突的信息；也不能因为保存过旧确认就执行新的参数。

### 12.3 记忆与数据保留

第一版只实现会话内任务记忆，不默认构建跨会话用户画像。持久化只保存任务必要信息、结构化消息、证据 ID 和执行结果引用，不把每次整页图、全部检索内容与模型内部推理反复写入 checkpoint。

设定可配置的会话、上传、run 日志保留时间；演示默认短期保留，正式环境经实际隐私要求确认后配置。删除会话需要删除或失效私有附件、授权缓存和检索条目，并记录删除执行状态。

### 12.4 意图感知需求与职责边界（首版部分实现）

**目标**：持续保留用户当前有效诉求，识别其在多轮中的变化；不遗漏、不擅自增加，不把已暂停或撤回的诉求继续当作办理依据。第一版是逻辑独立的进程内理解模块，不要求另建微服务。

首版实现和未关闭事项见 [意图实现记录](docs/intent_implementation.md)。以下为需求登记时的代码基线，不表示首版能力尚未接入。

原状：`Sessions.submit` 保存最近三轮问答，`AgentRunner._view` 提供最近两轮历史；现有 `PlanDecision` 每步选择动作，尚无独立意图库、意图变更协议或持久多诉求状态。已有会话恢复、商品切换不能替代暂停与恢复诉求。代码分析见 [意图理解设计](docs/intent_understanding_design.md)。

首版覆盖商品使用咨询、故障排查、售后规则咨询、售后办理请求、进度查询和转人工。会员退款及自动续费是协议说明/测试示例，不隐含新增会员系统或真实退款工具。意图目录须注明下游能力是否支持，识别正确不表示业务已可办理。

| 责任 | 必须完成 | 边界 |
|---|---|---|
| 理解服务 | 诉求、对象属性、对话行为、表达状态、变化、歧义和来源 | 不执行工具，不输出业务成功结果 |
| 状态管理 | 校验变化、合法迁移、幂等、版本和原文依据；持久保存诉求 | 模型不得每轮任意重写全部状态 |
| 对话管理 | 根据缺口决定何时追问、合并问题、组织回答或转人工 | 已明确的咨询不得无故索取办理字段 |
| 业务执行 | 现有授权、参数绑定确认、执行账本和幂等控制 | 意图、恢复诉求或“确认”标签都不是执行凭证 |

### 12.5 意图库与结构化协议（新增，待实现）

目录使用稳定 ID 与版本，记录定义、包含/排除样例、混淆项、业务对象和属性、支持范围及审核状态。区分“退款规则咨询”“申请退款”“查询退款进度”；年度/月度等属性不机械扩展成标签组合。目录新增、合并、拆分或定义修改须人工审核、回归并版本发布，不在线自动发布新标签。

输入至少包含当前用户原文、带角色与稳定 turn_id 的相关历史、最近客服提问、已有状态、taxonomy_version 和 expected_state_version。辅助改写不得替换原文或抹去否定/条件；不得使用后续轮次信息。上下文截断须保留未结束诉求、约束与指代所需来源，不能只依靠固定最近两轮。

输出契约至少包含：

- `intent_updates`：独立 goal_id、目录 intent_id、add/correct/suspend/resume/withdraw、原文跨度及 source_turn_id。
- 对象与属性：值、来源轮次、用户明确表达/模型推测；用户明确纠正可覆盖旧推测，未涉及字段保持原状。
- 对话行为及表达状态：追问/确认/否定/纠正，与当前请求/条件/假设/历史/被否定表达分开；非当前表达不自动成为待执行业务。
- 多诉求与歧义：多个真实目标存为不同 goal；一个模糊目标的候选解释另存，不能全部激活。只有明确表达顺序才记录先后关系。
- `constraints`、`clarification` 与未识别类型：区分语义歧义、对象不明、业务字段缺失、超出服务范围和目录未覆盖。输出建议澄清的区别及来源，不自称已执行。
- 版本与审计：turn_id、状态前后版本、taxonomy_version、模型/提示版本、用量及失败原因。自报 confidence 不作为校准概率或统一阈值依据。

### 12.6 多轮状态与执行安全（新增，待实现）

每个 goal 保留 active/suspended/withdrawn 等理解状态及变化历史；业务办理进度单独存放，不混成意图完成状态。合法转移由程序管理：仅对现有目标暂停/撤回，resume 仅恢复 suspended；撤回目标不得因模糊“继续”自动复活。对象纠正不等于换诉求；“另外”新增不等于替换前项。

例：“帮我申请退款”→新增申请；“先别申请，金额怎么算？另外自动续费在哪关？”→暂停原申请、新增两项咨询、保留禁止提交约束；不得提交退款或关闭续费。“不用了”必须绑定最近客服提问：拒绝重复解释不能据此撤回退款申请。

按主体、会话和商品任务隔离状态；切换商品不把旧对象、证据或授权带入新任务，跨任务恢复必须显式解析并重新核对范围。使用预期版本与轮次幂等键原子提交；相同轮次相同输入不重复应用，不同输入或过期版本返回冲突。重启恢复不丢失暂停项和约束。

理解失败时保留旧状态作为历史并标记本轮未完成，不能沿用旧 active 目标继续写操作；当前消息可能是在撤回。暂停/撤回要使未执行的旧确认失效，并在提交前核对最新状态；已经发生的副作用须查执行账本，不声称被撤回消息自动回滚。

### 12.7 实施顺序与接入（新增，待实现）

1. 发布首版意图规范与标注指南；业务/开发独立标注同批样例并讨论分歧，先消除定义重叠。
2. 实现类型契约、状态更新器、原文依据校验、持久化迁移、并发幂等及回放测试。
3. 以一次受预算约束的模型调用输出本轮变化，不把各属性拆成多次调用；适配现有 gateway，模型可替换。格式/语义失败不静默更新状态。
4. 接入 Sessions 与 AgentRunner；理解调用通过 MeteredGateway 计费，仍预留回答/核验预算。固定 B1/B2 不默认增加调用；版本变化须冻结新配置。
5. 前端显示必要澄清和可理解的当前任务信息；内部状态、模型分数不直接充当用户界面或授权按钮。
6. 执行独立多轮验证并报告负例；稳定后再评估候选检索、轻量模型分流及离线意图发现。首版不训练专用小模型、不做自动意图发布。

工期与请求量待目录规模、样本集和存储设计冻结后估算，不套用历史 M4 的已用工期。真实实验沿用 §23.3 及用户已授权预算，本需求登记不增加调用额度。

### 12.8 评测与退出要求（新增，待实现）

样本单位为对话前缀：历史、当前句、前状态、预期意图/对象/变化/约束、澄清需求及原文依据。按完整会话及相近合成改写分组切分，人工复核真实对话测试集；当前轮标注不能读取未来信息。fixture 测试与真实识别效果分开报告。

固定模型、目录、样本及资源条件比较“只看当前句”“当前句+历史”“当前句+历史+结构化状态”；分别报告实际输入长度、调用和成本，不把更大预算当成状态设计的效果。若以后引入候选检索，单独报告候选召回与候选内分类表现。

报告类别宏 F1/混淆对、多诉求整组匹配及漏报/多报、对象和状态迁移正确性、未知误接收/已知误拒绝、澄清必要性及解决率、自动理解覆盖率与其准确率、延迟和每轮用量。不能只以高覆盖率或频繁追问带来的低误判宣称成功。

退出需 INT-01 至 INT-12 通过、真实评测完整且失败计入分母、人工复核且可追溯；在开发集建立基线后、正式测试前冻结分项改进目标和成本上限。被测暂停/撤回/否定及授权负例不得发生误执行，未解释失败阻塞该能力验收。无论通过多少程序测试，未完成真实多轮效果验证仍标“未验证”。

### 12.9 按能力委派的主从 Agent（首版已实现，效果待验证）

修复更新：工单业务上下文已独立传入 Writer/Verifier，并重新校验查询作用域。旧失败样例的最终化真实复验通过（2 次请求，累计 869/870）；不是完整主从重跑，见 [修复记录](docs/business_context_repair.md)。

历史真实验证：两场景共 13 次模型请求，累计 867/870。纯查询通过；混合任务已执行主从委派，但业务查询事实未进入最终 Writer 上下文，最终核验失败。见 [真实验证记录](docs/supervisor_real_validation.md)，完整联验保持未通过。

第二轮已补有效目标与角色绑定、重复委派拦截、复用证据回传、受控失败报告，以及工单查询与其他问答的结果保留和继续处理。相关测试证明工程行为；完整多轮效果仍未验收。

用户新增需求：对不同客服能力适当引入主从架构。主控保留意图、会话、预算、授权与最终答复；指南、排查、规则咨询为受限只读子 Agent，商品确认/工单查询保持工具，售后提交保持确认链路。人工交接仍暂停。

首版独立角色指令、只读白名单、委派状态、返回报告、共享预算与中断恢复已接入。每轮最多两次委派、每次两次决策，不增加总调用额度；主控可跳过委派直接处理简单问题。详见 [主从 Agent 记录](docs/specialist_agents.md)。

验收 MA-01：主控委派后回收并执行最终核验；MA-02：子 Agent 不越权调用业务工具、跨范围证据或其他角色工具；MA-03：全局预算、取消、局部步数与递归限制；MA-04：中断恢复不重复执行、不丢委派状态；MA-05：真实分工与完整客服场景联验，报告委派正确性、未完成诉求、延迟与费用。主从结构按用户要求作为既定架构，不以单 Agent 对照决定是否保留；效果提升仍须实测依据。工程测试通过不代表 MA-05 或总体效果通过。

### 12.10 意图候选检索接入关系（新增计划，待实现）

§8.8–8.9 的 LLM Judge 接替当前 understand 的候选判决输入，仍输出现有 Delta 及原文依据，交给版本化 reducer 原子更新；不新建另一份单意图 DialogueState，也不在主/子 Agent 内重复跑完整意图管线。候选、槽位与分数仅供理解，不能成为 manual evidence 或业务确认。

新增分支共用总时延/模型调用预算；本地分类与精排记录耗时，远端模型调用统一记账。故障走已发布的降级 profile 或明确理解失败路径，并沿用 intent_uncertain 的写操作保护。当前一次 LLM 意图调用可替换输入，不默认再增加第二次 Judge；训练、建库、真实验证均需独立冻结资源计划。

---

<a id="s13"></a>
## 13. API、事件流与前端交互

### 13.1 API 契约（计划新增）

所有接口使用 `/v1` 版本前缀。请求和响应输出 JSON Schema/OpenAPI；敏感字段由服务端注入。

| 方法与路径 | 用途 | 要点 |
|---|---|---|
| `GET /v1/catalog/purchases` | 当前用户模拟已购商品 | 分页、权限过滤；不允许任意 user_id 查询 |
| `POST /v1/sessions` | 创建会话 | 返回会话 ID、revision、资料域 |
| `GET /v1/sessions/{id}` | 恢复会话 | 严格主体校验；不返回内部推理 |
| `POST /v1/sessions/{id}/product` | 确认/切换商品 | 乐观版本校验；不得借此越权 |
| `POST /v1/uploads` | 上传用户图片 | 文件类型、大小、像素和安全检查；返回 upload_id |
| `POST /v1/sessions/{id}/messages` | 提交消息 | `client_message_id` 幂等，返回 `202` 与 run_id |
| `GET /v1/runs/{id}` | 查询状态与最终答案 | 可恢复拉取；状态不夸大完成情况 |
| `GET /v1/runs/{id}/events` | 订阅 SSE | 事件 ID、有序、可断线续传 |
| `POST /v1/runs/{id}/cancel` | 取消 | 发取消信号；已完成副作用不伪装回滚 |
| `POST /v1/runs/{id}/confirm` | 确认模拟操作 | 验证一次性确认令牌和当前参数 hash |
| `GET /v1/assets/{id}` | 读取原图/裁剪图 | 同源认证，不暴露本地路径 |
| `GET /v1/sources/{id}` | 查看适用来源 | 版本、页码/时间码、授权的文档查看入口 |
| `POST /v1/feedback` | 反馈回答/配图问题 | 关联 run、step、reason；不自动改训练集 |
| `GET /health/live` | 进程存活 | 不要求全部模型在线 |
| `GET /health/ready` | 可接受新任务 | 检查 DB、索引快照与必要模型能力 |

管理员导入接口与普通聊天接口分离，默认仅本地开发管理入口可用。不能允许普通用户通过输入一个 URL 命令后台抓取内网资源。

### 13.2 请求示例（虚构演示 ID）

```json
{
  "client_message_id": "msg_demo_001",
  "expected_session_revision": 3,
  "text": "第二步提到的接口在哪里？",
  "attachment_ids": [],
  "reply_to_step_id": "step_demo_02"
}
```

认证主体、商品授权和资料域不从这段消息随意覆盖。

### 13.3 SSE 事件

事件至少包括：`run.accepted`、`scope.resolved`、`tool.started`、`tool.completed`、`verification.started`、`answer.ready`、`clarification.required`、`run.failed`、`run.cancelled`、`run.completed`。

每条事件含 `event_id/run_id/seq/timestamp/type/payload`。事件可重放，客户端按序号去重。`answer.ready` 只发送通过必要核验的结构化答案；早期事件只展示行动状态，不直接流出未核验的操作步骤或未经验证图片。

浏览器使用同源认证 cookie 建立 SSE，或采用能附带认证头的 fetch-stream；不能把长期 token 放 URL 查询参数。断线后通过 Last-Event-ID/游标续传；续传只重放事件，不重新执行 run。

### 13.4 前端功能

| 区域 | 必须具备的交互 |
|---|---|
| 商品栏 | 当前商品、型号、选择/切换、未知状态提示 |
| 聊天区 | 多轮消息、上传、等待状态、重试与取消 |
| 步骤卡片 | 连续编号、文字、关联图片、局部追问入口 |
| 原图卡片 | 来源名称、页码、放大、整页查看、加载失败提示 |
| 引用面板 | 文档版本、商品适用范围、可跳转原始证据 |
| 行动记录 | 简短展示查询/看图/核验，不展示内部敏感信息 |
| 反馈入口 | 答非所问、型号不符、图片错误、步骤不清晰 |

移动端保证长步骤可阅读，图片不强制压成无法辨识的小缩略图；键盘操作、焦点、alt 文本和错误提示纳入测试。来源图片中的文字不能全部依赖 OCR alt 文本替代。

### 13.5 输出安全

前端不渲染模型提供的任意 HTML、远程 `img src`、JavaScript 或可执行 Markdown。图片和来源由后端资产 ID 构造；文本做安全转义。页面引用失效时显示失效状态，不继续使用浏览器过期缓存掩盖服务端撤销。

---

<a id="s14"></a>
## 14. 安全、隐私与内容边界

### 14.1 认证与权限

开发模式可使用固定模拟用户，但只绑定本地地址并显示 DEMO 标志；非本地部署禁用匿名模拟认证。用户身份来自服务端会话/认证中间件，不来自 Planner 参数。

对订单、会话、上传、资产、run、事件流逐一做对象级授权。共享公共手册与私人上传分域，私有文件不进入公共向量索引。缓存键包含主体/授权集合和数据代次，防止跨用户命中。

### 14.2 文件与网络输入

拟议默认上限：用户图片每文件 20MB、最多 25MP；管理员 PDF 每文件 100MB、500 页。数值可配置，超限拒绝或转离线处理，不让模型决定是否放行。

必须检测 MIME 与文件签名、压缩炸弹、异常尺寸、路径穿越、软链接逃逸、恶意 SVG/HTML、PDF 附件/脚本和解析超时。未知文件进入隔离区。私有用户图片的展示派生图移除不必要 EXIF；原始文件若保留则加访问控制和保留期限。

资料下载使用域名/协议白名单、重定向检查、私网地址拦截、长度限制和超时。模型不持有任意 HTTP、shell、SQL 或文件系统工具。

### 14.3 提示注入

手册、OCR、截图、评论、工具返回全部作为低信任数据。测试包括“忽略规则”“发出管理员工具调用”“读取其他用户订单”“泄露密钥”等内容。必须验证其不能改变系统提示、工具权限、SearchScope 或输出资产来源策略。

不把低信任检索内容拼接成 system message。工具结果与操作指令使用不同消息角色和字段；无论模型是否遵从恶意文字，确定性权限边界必须阻止越权。

### 14.4 写操作边界

产品首版真实写操作全部关闭；模拟业务按明确环境和政策执行。确认必须包含对象、行为、影响和参数，不把泛泛的“好的”认作对另一项操作的同意。已发生效果不能只靠删除聊天记录回滚。

### 14.5 模型数据外发

模型服务是外部端点时，展示相关说明；只发送必要图片和文本，不发送整份订单库、无关个人信息或隐藏评测信息。记录服务商/model ID 和请求用途，不在日志中存 API key、cookies 或可长期访问私有图片的 URL。

### 14.6 许可与发布

代码、权重、说明书、图片、视频、品牌资产分别审查。官方代码开源不等于所有来源资料可公开再分发。公开仓库只放允许分发的 fixtures、下载说明、转换脚本和去敏后的指标；不能上传整个第三方数据包、原始客服内容或模型权重。

---

<a id="s15"></a>
## 15. PM209 评测实施协议

### 15.1 先验证评分，再验证模型

编写独立 `PM209Adapter`，输入为清洁的 corpus 和当前问题，输出不含 gold 的预测。评分器在独立进程加载答案、相关区域和官方 ID 映射。

评分 adapter 不能直接复用会把 `answer.text/answer.relevant` 一起送入模型的训练 loader。官方 loader 可作为格式参考，但在线输入必须按字段白名单重新构造。[S09]

### 15.2 四种设置不得混合

| Profile | 允许输入/访问 | 作用及报告标识 |
|---|---|---|
| `pm209-given-page` | 问题 + 已知正确页面 + 允许的文档区域结构 | 阅读/选区诊断，明确 gold-page，不叫检索端到端 |
| `pm209-retrieved-top1` | 问题 + 正确手册范围，先排序页面，再只读取 top1 | 对齐原始 retrieved-page 路径，记录其具体预算与配置 |
| `pm209-agent-multipage` | 问题 + 手册范围，在共同预算内主动检索和读多页 | 产品主实验，标记扩展协议，不能冒充原论文同设置 |
| `shopguide-cross-product` | 模拟订单、商品歧义和多手册 | 工程/扩展集，不属于 PM209 官方评测 |

官方源码逐手册执行页面检索，并支持用检索页面映射替换原正确页面；这与无约束全库检索不是同一个任务。[S20] 所有报告保存 `given_information`，明确是否预先知道手册和页面。

### 15.3 预测记录

```json
{
  "schema_version": 1,
  "dataset": "pm209",
  "split": "test",
  "protocol": "pm209-agent-multipage",
  "question_id": "opaque_question_id",
  "manual_id": "opaque_manual_id",
  "retrieved_page_ids": ["page_demo_01", "page_demo_02"],
  "visited_page_ids": ["page_demo_01"],
  "answer_text": "This field contains only the predicted answer.",
  "predicted_region_ids": ["region_demo_01"],
  "display_asset_ids": ["asset_demo_01"],
  "status": "answered",
  "run_id": "run_demo_pm209",
  "error": null
}
```

不得在预测文件中附上 gold 后又把同一个文件喂给 Agent。问题 ID 由 adapter 映射到官方枚举编号；`predicted_region_ids` 是答案实际使用的证据区域，不是所有检索候选。

### 15.4 官方指标

- 页面检索：保留官方实现的 Recall@1/3/5、聚合方式与候选范围；多页 Agent 另报初次检索 Recall 和预算内访问命中，不换名冒充同指标。
- 文字：调用锁定版官方文字评分或经过 parity 的兼容包装，保持标点处理、小写化、NLGEval 配置等差异可追踪。[S20]
- 区域：报告官方 `instance_precision/recall/f1` 与 `all_precision/recall/f1`，分别对应其逐实例与整体聚合实现；不只挑更高的一项。[S20]
- 类型：保留各区域类型分组；配图能力重点另报 Product Image、illustration、graphic，Table 独立统计。[S10]

NLGEval 等资源无法运行时明确报告缺项及原因；替换为 ROUGE 或 LLM judge 只能标注为替代指标，不能沿用原指标名称。

### 15.5 产品扩展指标

这些是项目新增，不称为官方 PM209 指标：

| 指标 | 定义与注意 |
|---|---|
| `strict_region_f1` | 用完整 gold 区域集合与实际预测区域计算，不让错误页面缩小 gold 分母 |
| `display_image_precision` | 实际展示的合法图块中，与参考图块相关的比例；空预测单独处理 |
| `source_validity` | 展示/引用资产是否存在、属于当前范围且来源 hash 可核对 |
| `wrong_product_rate` | 型号测试集上发生越商品资料引用的比例 |
| `answer_coverage` | 可回答样本中实际回答的比例，与选择性准确性同时报告 |
| `unsupported_claim_rate` | 独立审核认为无依据的关键结论比例；不是自身 verifier 通过率 |
| `cost` | 模型请求、输入/输出 token、图像输入、工具次数和总耗时 |

当旧评分器只在预测页面的候选区域上计算标签时，需审查错误页面情况下的分母处理；官方分数仍按原实现给出，另外用 `strict_region_f1` 防止漏算真实未命中证据，不悄悄改官方评分代码。

### 15.6 Parity 测试

用手工构造、明确属于测试代码的微型真值覆盖：完美预测、全空、全错、重复区域、未知 region ID、错误页面、多个正确区域、类别缺失和重复 question ID。

原 scorer 与兼容 scorer 对相同合法输入必须在预先定义容差内一致；非法 ID 在评测前标记错误且不可被静默过滤为更好成绩。源代码迁移需保存补丁和 scorer hash。

### 15.7 基线与消融

| 版本 | 设计 | 要回答的问题 |
|---|---|---|
| B0 | 正确页给定的 VLM/URA 诊断 | 不考虑检索时阅读/选区能力怎样 |
| B1 | 固定的文字 RAG | 现有检索思路迁移后的基线 |
| B2 | 固定图文 RAG，相同模型与资产库 | 增加视觉输入/选图带来多少收益 |
| B3 | 主动 Agent，与 B2 相同工具和累计预算 | 动态决策是否有独立价值 |
| B4 | 可选 SFT/RL 主控 | 训练是否进一步改善策略 |

B0 不能作为 B3 的同等输入对比。B2/B3 在 multipage profile 中使用共同页面、图像和成本预算；端到端最多执行多少次模型调用必须公开。消融包括无型号硬过滤、无视觉核验、无相邻步骤补读、无预算/循环保护；涉及取消安全控制的消融只在隔离测试中进行，不用于用户演示部署。

### 15.8 正式运行步骤

`数据审计 → scorer parity → train/val 开发 → 50 个 val 冒烟 → 模型/配置/预算冻结 → 完整 test → 失败统计 → 报告与复现包`。

50 是拟议冒烟规模，不是删减正式测试集。完整运行应包含所有预定测试问题；失败和拒答保留在分母。只在证据充分时排除数据损坏，并同时公开原始分母、排除规则、排除数量和严格全量结果。

---

<a id="s16"></a>
## 16. ECom-Bench 评测实施协议

### 16.1 接入不改任务含义

复用固定版官方用户模拟器、业务政策、工具服务和判定逻辑，替换 Agent 决策实现。主服务与 benchmark 分开启动，Agent 不具备 benchmark 目录的任意文件读取或 Python 执行权限。

先完成原生 schema 到模型网关的适配，再添加 ShopGuide 的检索/核验能力。主评分仍取官方环境执行结果，不用我们自己的答案 judge 替代。

### 16.2 配置与可比性

论文设置包含 20 轮、600 秒任务限制及具体用户/视觉模型配置；M0 必须核对固定代码的参数含义，尤其区分用户回合和内部 tool step。[S11] 老模型服务不可用时允许替代，但报告需注明“协议适配实验”，重跑所有比较基线，不能与原论文数值直接宣称持平/超越。

冻结：任务列表、环境 SHA、政策 hash、Agent/user/vision model revision、提示模板、采样参数、轮数、工具预算、时间限制、随机种子和缓存策略。

### 16.3 分数

固定版环境的最终 reward 为：

```text
success = action_success × required_search_success × output_success
```

`reward_time` 单独记录，不加入最终 reward 的乘积。[S12]

对每个任务运行 n 次，成功次数为 c：

```text
pass^k = mean_over_tasks( C(c, k) / C(n, k) )，n ≥ k；c < k 时该任务项为 0。
```

这是多次都成功的可靠性估计，不是至少成功一次的 `pass@k`。[S11]

拟议正式设置 n=8，报告 pass^1/pass^3/pass^5/pass^8，并报告失败维度和置信区间。53 个任务全部可用时为 424 次任务运行，实际成本需先用小样本计量；预算不足先跑 n=3 并明确只能报告相应 k，不推算更高 k。外部模型费用需获得项目负责人的运行预算确认。

### 16.4 运行隔离与超时

每次 trial 使用独立临时目录、MCP 服务和 session namespace，结束后保存必要结果并清理。不同任务不共享订单变更、对话记忆或完整响应缓存。

源码中存在对空回复继续重试的循环，以及对服务消息轨迹的特定读取路径。[S12] 运行器必须设置外层有界 watchdog，记录任务失败而非无限等待；涉及修复的变更保存补丁并应用于全部比较方法，不能仅对自己的 Agent 放宽条件。

### 16.5 指标边界

必须报告 action/search/output 原生检查的含义。关键词通过不等于语义完全正确，必要工具调用通过也不意味着检索相关性达到某个 precision；不要把它们改名为“回复准确率 100%”或“检索精度”。

另外报告真实延迟、工具次数、token、错误参数、无效调用和实体混淆等诊断项。此处的图片任务不能代替 PM209 原图选择指标。

### 16.6 开发与最终报告

1. 用独立合成环境测试 adapter 的序列化、schema、权限与写入流程。
2. 登记少量官方 smoke/dev 任务；之后调参不得偷看冻结 holdout。
3. 对所有预定任务运行 B2 类固定策略和 B3 Agent，保持用户模拟模型相同。
4. 不筛掉工具故障、超时或低分任务；区分基础设施错误与能力错误，并提供总体结果。
5. 对同任务的配对方法做按任务聚类 bootstrap；不同 trial 不是互不相关的新任务样本。
6. 报告全量评测与 holdout/已调试任务结果，不把公开任务上的过拟合称为未见场景泛化。

---

<a id="s17"></a>
## 17. 测试分层与详细测试矩阵

### 17.1 测试层次

| 层 | 目标 | 模型/网络 | 每次 PR |
|---|---|---|---|
| 单元 | 数据类型、过滤、坐标、预算、幂等逻辑 | 全离线 fake | 必跑 |
| 契约 | API/schema、工具注册、消息和评测转换 | mock HTTP/MCP | 必跑 |
| 集成 | SQLite/LanceDB/文件/状态图真实协作 | 本地服务，模型 scripted | 必跑小规模 |
| 安全 | 越权、注入、上传、执行边界 | 离线为主 | 必跑 |
| 浏览器 E2E | 商品选择、图文回答、追问、恢复 | 测试后端 | 必跑核心路径 |
| 模型 smoke | 真实工具调用、多模态读取、结构化输出 | 真实配置模型 | 合并前或人工批准 |
| 基准评测 | PM209 / ECom 实际效果 | 官方数据与真实模型 | 里程碑/发布时 |
| 性能/恢复 | 并发、超时、重启、资源 | 指定机器 | 每次发布 |

### 17.2 单元、数据与存储测试

| ID | 用例 | 关键断言 |
|---|---|---|
| U-01 | Product/Variant 字段非法 | 缺字段、未知字段、错误类型被拒绝 |
| U-02 | 同品牌相似型号 | 不自动等同；解析结果为候选/澄清 |
| U-03 | 文档/视频 locator | 判别类型正确；不能混用页码与时间 |
| U-04 | bbox 边界与随机生成 | 越界/零面积拒绝；合法框裁剪面积为正 |
| U-05 | 旋转/非零 CropBox | 正向与逆向坐标转换在预定像素容差内一致 |
| U-06 | asset 来源链 | 任意显示图能追溯到字节 hash 与父来源 |
| U-07 | 文本/向量数量不匹配 | 入库失败，不创建 ACTIVE 索引 |
| U-08 | 向量 NaN/Inf/维度错误 | 健康检查失败 |
| U-09 | 证据去重和顺序 | 同版本同 ID 去重；不同版本不错误覆盖 |
| U-10 | 参数规范化 | 等价 JSON 得到相同 hash；不同参数不误合并 |
| U-11 | budget 预留 | 嵌套调用计费；耗尽时不再发请求 |
| U-12 | no-progress 检测 | 重复无新增证据的调用被有界停止 |
| U-13 | AnswerRenderer | 拒绝任意图片 URL/HTML，接受合法 asset ID |
| U-14 | 官方 question/region 映射 | 原始顺序不变；往返转换不丢 ID |
| U-15 | 区域指标 | 完美/空/错页/重复预测与预期一致 |
| U-16 | pass^k | c=n 得 1；c<k 得 0；n<k 拒绝；不误算 pass@k |
| U-17 | config 校验 | 未填模型 revision/正式 data manifest 无法运行正式评测 |
| U-18 | 上传路径和尺寸 | 路径穿越、压缩炸弹、伪造 MIME 被拒绝 |

### 17.3 集成与工具契约测试

| ID | 用例 | 关键断言 |
|---|---|---|
| I-01 | 导入→索引→查询→展示图 | metadata、页码、hash 和图像字节完整 |
| I-02 | 重启重开索引 | Product/版本/区域/权限字段不丢失 |
| I-03 | 前置 scope 过滤 | 更相似但不授权的商品不进入候选 |
| I-04 | FTS 与 dense 分别过滤 | 两路结果都满足相同 scope |
| I-05 | 新文档代次激活 | 活跃 run 固定旧快照，新 run 使用新快照 |
| I-06 | 导入中断与恢复 | 不重复资产，不激活半成品 |
| I-07 | 图文不一致 | 核验发现问题并触发有界修复/降级 |
| I-08 | 缺图 | 返回图缺失提示；不存在生成图片调用 |
| I-09 | 实际图像输入 | mock gateway 捕获到图像字节/受控图像输入，不只有 caption |
| I-10 | 用户照片 | 上传图只在本用户会话使用，不进入公共证据 |
| I-11 | session checkpoint 恢复 | 任务、商品和消息不重复、不丢失 |
| I-12 | 客户端重复消息 | 同 client_message_id 返回同 run，不重复操作 |
| I-13 | 模拟写操作超时 | 先查询幂等结果；未知状态不自动重做 |
| I-14 | 旧确认令牌 | 参数变化/过期/主体变化后拒绝 |
| I-15 | ECom 工具序列化 | 原生工具名/参数/实际调用完整进入评分日志 |
| I-16 | ECom trial 隔离 | 上一任务修改不影响下一任务 |
| I-17 | API/SSE 断线 | 事件续传不触发重复模型/工具执行 |
| I-18 | 模型 429/坏 JSON | 有界重试；明确错误；不循环到进程卡死 |

### 17.4 安全测试

| ID | 攻击/故障 | 关键断言 |
|---|---|---|
| S-01 | 猜测他人 order/session/run ID | 返回未授权/不存在，不泄露详情 |
| S-02 | 猜测 asset ID 或复用旧链接 | 重新授权，不能凭 ID 读取 |
| S-03 | 手册中提示“忽略规则” | scope 与工具权限不变 |
| S-04 | 用户图中的指令 | 不触发管理员工具或任意网络请求 |
| S-05 | 参数注入 SQL/路径 | schema/白名单拒绝，不拼接执行 |
| S-06 | 外部 URL 指向私网/多次重定向 | 下载器阻断 |
| S-07 | 私有缓存跨用户复用 | 缓存隔离，无数据泄露 |
| S-08 | 模型构造不存在图片 | 核验失败，原图接口不提供替代生成图 |
| S-09 | 资料撤销后缓存命中 | 输出前拒绝被撤销证据 |
| S-10 | 诱导现实交易/退款 | demo 只影响模拟环境，不发真实请求 |
| S-11 | 日志含 key/cookie/私人图片地址 | 脱敏检查失败并阻止发布 |
| S-12 | Agent 访问 gold/任务 metadata | 沙箱/挂载和消息白名单阻断 |

### 17.5 产品 E2E 测试

每条用例使用测试 fixtures 的确定性预期，不依赖随机 LLM 回复的精确措辞。

| ID | 前置与输入 | 执行 | 验收 |
|---|---|---|---|
| E-01 | 用户甲有两个相似型号 | 问“怎么使用这个功能” | 先澄清；确认后只有对应型号证据 |
| E-02 | 单个确认商品，有完整图文资料 | 提问操作流程 | 显示步骤和正确原图，来源可打开 |
| E-03 | E-02 后 | 追问“第二步在哪里” | 保留任务，指向正确 step/region |
| E-04 | 当前设备界面与资料冲突 | 上传不同版本截图 | 提示差异并确认，不继续错误指导 |
| E-05 | 先问商品 A，再明确换 B | 继续问“怎么连接” | B 的资料，不能显示 A 的旧图 |
| E-06 | 有文字证据但图文件丢失 | 执行回答 | 明确缺图，文字有据，未显示虚假图 |
| E-07 | 来源不足 | 要求给出非常具体操作 | 标记不足，不拼接相似型号方法 |
| E-08 | 同会话两个并发提交 | 连续发送不同消息 | 排队或冲突，不混合状态 |
| E-09 | 模拟工单待确认 | 确认并网络重发 | 只创建一张工单，返回真实编号 |
| E-10 | run 执行中后端重启 | 重新打开会话 | 恢复或明确失败；不重复副作用 |
| E-11 | 私有上传图 | 切换到另一用户访问 | 图/会话不可访问 |
| E-12 | 手机窄屏与键盘导航 | 浏览步骤、放大图、返回来源 | 图文可读、焦点和 alt 信息有效 |

### 17.6 基准与数据泄漏测试

| ID | 用例 | 关键断言 |
|---|---|---|
| B-01 | 扫描 corpus/export | 不含 answer、relevant、gold page 映射 |
| B-02 | scorer parity | 兼容评分与官方合法输入结果一致 |
| B-03 | 未知/重复 prediction ID | 标记异常且不静默缩小分母 |
| B-04 | given-page vs multipage | profile 和模型可访问页面集合严格不同 |
| B-05 | 文件名带提示 | 对模型暴露的路径已不透明化 |
| B-06 | fake 模型请求正式评测 | 启动失败，不能输出“正式分数” |
| B-07 | PM209 返回全部候选作答案 | 精确率按协议受罚；实际引用范围可核对 |
| B-08 | ECom 缺失工具调用日志 | 评分无效/失败，不由预期任务补写 |
| B-09 | 外部资源缺失 | coverage 下降明确报告，不悄悄跳题 |
| B-10 | 重新运行相同 fingerprint | 数据/配置一致，差异有种子/服务说明 |

### 17.7 性能与稳定性测试

| ID | 场景 | 记录 |
|---|---|---|
| P-01 | 热索引固定 scope 检索 | p50/p95/p99、候选数、数据规模 |
| P-02 | 冷启动/首次模型加载 | 启动时间、首次查询耗时，与热态分开 |
| P-03 | 1/5/10 并发会话 | 完成率、队列等待、延迟、错误率、内存 |
| P-04 | 30 分钟连续请求 | 资源增长、会话锁、文件句柄与临时文件 |
| P-05 | 模型超时、限流、断网 | 有界响应与降级，不挤占无限资源 |
| P-06 | 磁盘满/DB 锁冲突 | 事务失败处理、不产生损坏 ACTIVE 状态 |
| P-07 | 取消与客户端断开 | 后台是否正确取消/继续，成本有记录 |
| P-08 | 索引/服务回滚 | 正确版本组合、旧来源引用可解释 |

所有安全、恢复和工程测试用的 fixtures 是项目测试资料，不计入 PM209/ECom 的样本量或论文结果。

---

<a id="s18"></a>
### 17.8 意图感知专项测试（新增，均待实现/执行）

| ID | 场景 | 必须断言 |
|---|---|---|
| INT-01 | 退款规则/申请/进度、业务属性变化 | 诉求区分正确，属性不制造标签；识别不假称工具已支持 |
| INT-02 | 相同“不用了”，不同客服提问 | 只否定对应提问，不误撤回其他诉求 |
| INT-03 | “不是退款，是问规则”、条件/假设/历史 | 否定目标不激活，条件未成立不执行 |
| INT-04 | “查物流，另外问退货方法”与模糊“处理订单” | 多诉求与候选歧义分开，无凭空优先级 |
| INT-05 | 暂停→咨询→恢复、撤回→模糊继续 | 保留目标 ID 和历史，禁止无依据复活撤回项 |
| INT-06 | “不是这个订单”、指代两个对象 | 显式纠正优先，仅修改有依据字段，必要时澄清 |
| INT-07 | 意图明确缺字段/超范围/新表达 | 不统一归其他，不为规则咨询无故索要办理信息 |
| INT-08 | 历史超过两轮、重启、切换商品/主体 | 保留活动诉求及约束，状态/证据/授权不串用 |
| INT-09 | 消息重发、同键异文、并发版本冲突 | 原子且幂等，不丢更新、不重复应用 |
| INT-10 | 非法标签/跨度/迁移、格式错误、超时、注入 | 拒绝非法更新；本轮未理解不能沿用旧状态提交 |
| INT-11 | 待确认时暂停/撤回、旧确认重放、已执行操作 | 提交前检查最新状态，旧确认失效，已发生副作用不伪装回滚 |
| INT-12 | 目录变更、三组消融、样本分组与真实中文测试 | 版本可追溯、无未来泄漏、完整报告效果/澄清/预算，程序与模型结果分开 |


### 17.9 意图候选检索与 RAG 改造测试（新增，均待实现/验证）

| ID | 场景 | 关键断言 |
|---|---|---|
| IR-01 | 原文/规范化、否定/条件/型号/数字 | 原文可回溯，跨度仍定位 raw_query，不错误合并表达 |
| IR-02 | 正负规则、撤回与多诉求 | 负词不是全局删标签，候选信号不直接执行 |
| IR-03 | BERT 未训练/标签版本变化/多标签 | 显式分支状态；无假分数；输出覆盖多诉求 |
| IR-04 | 样例聚合、少样例/并列/margin | 聚合可复现，缺失值不伪装零概率，不泄漏跨用户样例 |
| IR-05 | 融合尺度、分支故障、版本配置 | 保留分支证据和可用性，降级显式，不能原样混加异尺度分数 |
| IR-06 | 正确意图不在 Top3、多目标超候选上限 | 状态补充/候选不足路径可见，不硬选或默默漏掉目标 |
| IR-07 | 相似意图、多标签 hard negatives | 精排不把另一个有效诉求标负，训练数据不含测试组 |
| IR-08 | 最近问题的槽位回答、暂停恢复、改口 | 仍更新正确 goal，UNKNOWN/CLARIFY 与多个有效目标分开 |
| IR-09 | 意图引导检索、型号与私有数据 | 权限先过滤，意图/槽位不能扩大证据域或授权 |
| IR-10 | 多页证据截断及图文候选 | 相关页面进入上下文，配额/token 有界，不按 gold 保留；真实图来源不变 |
| IR-11 | 工单查询混合指南、置信度高但未确认 | 业务与手册来源分开，不能由分数触发写操作或取消确认 |
| IR-12 | 分层真实评测、消融、泄漏/成本审计 | 意图候选与文档候选分别报告；失败计分母，超预算停止，配置与模型可追溯 |


## 18. 测试执行、统计分析与质量门禁

### 18.1 一次标准开发测试循环

```text
阅读任务与验收条件
 → 写/补失败测试
 → 最小实现
 → 本模块单元测试
 → 契约与集成测试
 → 旧功能回归
 → 必要的真实模型 smoke
 → 保存测试报告、差异与风险
 → Code Review / 合并
```

修复缺陷时先加入能稳定复现的回归测试。禁止删除断言、跳过失败样本、把错误吞掉或用固定返回值伪造“通过”。模型效果失败与接口故障分开定位，不仅修改提示词遮盖工程错误。

### 18.2 必须产出的测试报告

每次正式测试保存：Git SHA、工作区 dirty 状态、数据/模型/config/prompt hashes、平台、Python/CUDA/驱动、命令、起止时间、退出码、通过/失败/跳过数量、日志位置和结果摘要。

`artifacts/<run_id>/` 至少包含：

```text
manifest.json
config.resolved.yaml
predictions.jsonl            # 基准运行时
events.jsonl                # 不含隐藏推理与密钥
tool_calls.jsonl
metrics.json
failures.jsonl
junit.xml                   # 工程测试时
summary.md
```

示例、stub、fake 与真实模型运行使用不同 `run_kind`。未收集到任何测试用例不能算通过；跳过原因必须可见。

### 18.3 质量门禁

| 门禁 | 拟议标准 | 说明 |
|---|---|---|
| 兼容性 | 现有测试和旧 CLI 冒烟均通过 | 已知外部依赖失败必须登记，不能直接移除旧功能 |
| 类型与风格 | 新模块 Ruff/Mypy 无未批准问题 | 不滥用 `Any`、`# type: ignore` |
| 单元覆盖率 | 新模块语句覆盖率 ≥80% | 不是正确性的充分证明 |
| 关键分支 | 权限、来源、幂等、预算分支覆盖率 ≥90% | 同时要求安全测试全部通过 |
| 原图来源 | 测试集合内展示资产的来源、范围和 hash 校验 100% 通过 | 仅指被测集合，不宣传绝对可靠 |
| 型号/授权 | 越型号、越用户负例全部阻断 | 任何未解释失败均阻塞发布 |
| 原图展示 | 核心 E2E 中可打开来源，缺失图明确降级 | 禁止生成图替补 |
| 评测一致性 | scorer parity 全部通过、预测 ID 覆盖完整 | 无 silently dropped cases |
| 容错 | 取消、重试、重启不产生重复模拟副作用 | 必测真实执行账本 |
| 数据隔离 | Agent 无 gold/任务真值访问路径 | 运行镜像、挂载、消息都检查 |
| 结果报告 | 所有预定问题/任务有记录，失败在分母 | 分母变化必须解释 |

效果门槛不预写“准确率达到 90%”。M3 跑出基线后，M4 开始前冻结实际改进目标和可接受成本。若 B3 不优于 B2，保留较简单方案作为默认并报告结果，不伪造提升。

### 18.4 性能目标与测量条件

拟议单机演示目标：API 入队 p95 ≤500ms、热索引检索 p95 ≤1.5s、5 并发下标准图文任务完成 p95 ≤60s，单轮超时有界；这些是目标，不是当前能力。

M0/M3 需补齐机器、模型服务、数据规模、输入长度、图像分辨率、缓存、网络和并发条件，然后在效果实验前冻结目标。未满足时先记录瓶颈与优化，不在跑完测试后临时修改门槛制造通过。

分别报告排队、检索、重排、视觉、Planner、Writer、Verifier 和渲染耗时。冷启动与热态分开，服务端处理与浏览器完整加载分开。

### 18.5 统计与公平性

- 同模型、同工具、同检索库、同调用预算进行 B2/B3 配对比较。
- PM209 按手册聚类 bootstrap，避免同手册大量相似问题造成过窄区间。
- ECom 按任务聚类，保留 trial 层结果；公开样本量与成功次数。
- 默认报告 95% 置信区间；同时给出绝对差、相对差、成本变化，不只挑最高单次结果。
- 模型服务即使支持 seed 也不必然完全确定，保存全部配置与运行差异。
- 拒答同时报告 coverage；不能全部拒答后声称无幻觉且效果最好。
- 若人工审核，先写 rubric，再随机抽样；尽量双人审核分歧。一个人审核则明确局限。

---

<a id="s19"></a>
## 19. 分阶段开发流程与交付物

### 19.1 排期基准

以一名熟悉 Python 的开发者持续投入为估算基准，建议安排 8–12 周；实际工期取决于数据下载、环境兼容、模型服务、前端经验和投入时间。这不是已经确认的人力或完成承诺。若多人协作，可把数据/评测、Agent/后端、前端/QA 分开，但接口由同一份契约维护。

| 阶段 | 预计工作日 | 前置 | 核心交付 |
|---|---:|---|---|
| M0 基线与可行性 | 3–5 | 无 | 仓库审计、两套数据可用性、评分器与环境验证 |
| M1 契约与基础设施 | 3–4 | M0 | schemas、V2 存储、fixtures、配置和工具 registry |
| M2 入库与有范围检索 | 5–7 | M1 | 手册/原图入库、索引、商品作用域、健康检查 |
| M3 固定图文 RAG 与 PM209 | 4–6 | M2 | 可运行 B1/B2、原图回答、首份真实指标 |
| M4 Agent 与多轮状态 | 5–7 | M3 | 主动工具循环、修复、预算、会话恢复 |
| M5 API 与交互前端 | 4–6 | M4；部分可提前 | 商品选择、SSE、步骤图、追问和授权接口 |
| M6 ECom 适配与完整评测 | 4–6 | M4 | 原生工具适配、trial 隔离、官方评分报告 |
| M7 加固、消融与发布 | 4–6 | M5+M6 | 安全/性能/恢复测试、对比报告、部署与回滚 |

合计 32–47 个工作日，另预留约 20% 风险缓冲，约为 8–12 个工作周。M0 数据或评分失败未解决，后续不能将该基准写为“已支持”。

### 19.2 M0：基线与可行性验证

**任务**

- [x] 审阅工作区，创建开发分支/独立 worktree，不覆盖未提交改动。
- [x] 保存现有 SHA、锁文件，实际运行旧测试与 CLI 冒烟。
- [x] 下载/审计 PM209，验证页面图与标签可读取。
- [x] 克隆并固定 ECom，枚举任务、工具、图像资源和模型需求。
- [x] 明确代码/数据/模型的许可边界，生成待授权清单。
- [ ] 运行最小 scorer parity 与至少一个官方环境基线 smoke。
- [ ] 验证 Planner 结构化输出与 VLM 实际读取图片。
- [ ] 记录设备、内存、显存、模型端点和单次调用成本，不推测已有 GPU。

**产物**：`baseline_audit.md`、`data_audit.md`、`model_manifest.md`、`benchmark_protocol.md`、首批 ADR、原始日志。

**退出条件**：已证明数据可用、评分器可运行或明确定位兼容修改；基础模型可供调用。无法完成时将该项列 BLOCKED，不能继续承诺正式基准成绩。

### 19.3 M1：类型、存储与测试框架

**任务**

- [x] 新增 `shopguide/` 和可选依赖，不改坏旧导入路径。
- [x] 实现 Product、DocumentVersion、Locator、Asset、Evidence、Action、Answer schema。
- [x] 建立 SQLite migrations、资产仓库和 mock catalog。
- [x] 建立 ScriptedPolicy/FakeVisionObserver，定义统一模型网关。
- [x] 实现 ToolRegistry、参数验证、ToolResult 错误类型。
- [x] 写坐标、权限、ID 和版本 fixtures；建立离线 CI。

**退出条件**：U-01 至 U-06、U-10、U-13、U-17 等基础用例通过；类型契约生成文档；旧测试通过。

### 19.4 M2：数据和检索闭环

**任务**

- [x] 分离 PM209 corpus 与 eval_private，保留官方映射。
- [x] 实现页面图导入、PDF 解析/渲染、区域来源与 deterministic crop。
- [x] 建立 STAGING/AUDITING/ACTIVE 流程与快照指针。
- [x] 实现 V2 scoped dense/FTS、候选融合与重排。
- [x] 保存图注/邻近文字关系及原图检索字段。
- [x] 实现重开索引健康检查、失败恢复与撤销处理。

- [ ] RAG 改造：建立独立的意图样例索引与版本清单；接入 Keyword/Embedding/Slot 候选，BERT 未训练时显式关闭（§8.8）。（内存精确索引、导出快照与三类信号已实现；索引加载复用、数据运营和完整验证待补。）
- [ ] RAG 改造：实现候选融合、状态保留与意图 Cross-Encoder 适配，保留逐层审计（§8.9）。（RRF、通用 BGE 重排适配、全目录召回保护及 trace 已实现；大目录缩减和专用训练未完成。）
- [ ] RAG 改造：优化资料 RAG 的页面/证据/图片预算分配并验证 IR-09/10，保留旧索引与实验。

**退出条件**：I-01 至 I-06、S-01/S-02/S-07/S-09 通过；对开发资料可查询原图和原页。不能只在 InMemory fake store 测试后宣布数据库已完成。

### 19.5 M3：固定图文问答与评测基线

**任务**

- [x] 实现 B1 文字 RAG 与 B2 固定图文 RAG。
- [x] 实现 Writer、RegionSelector、三层核验、结构化渲染。
- [x] 完成 PM209 不含 gold 的推理 adapter。
- [x] 完成 scorer parity 和三种 PM209 profile。（完整文字/区域参考验证已完成，三协议 B1/B2 各 50 题均已运行并评分；保留 tokenizer/环境适配说明，不等于完整 test 或模块验收通过。）
- [ ] 运行 val 冒烟与基线；审查错页、错图、错型号和缺图案例。（50 题三协议基线与描述性失败审查已完成；逐图/版本及完整产品场景复核、B2 可靠性仍待验收。）
- [ ] 在后续优化前固定数据/模型/提示/预算和效果目标。

- [ ] RAG 改造：验证上下文改造后的 Writer/Verifier 与独立业务上下文；分别复测意图层及证据层指标，不混用旧预测（§8.10–8.11）。

**退出条件**：回答确实使用图片、来源可追溯，PM209 原始协议分数与扩展分数分开，所有失败有记录。

### 19.6 M4：Agent、会话和恢复

**任务**

- [x] 替换固定流程入口为可选择动作的主控策略。
- [x] 实现商品确认、信息缺口、工具观察、候选答案和 repair 状态。
- [x] 实现预算预留、重复调用保护、取消和限流。
- [x] 增加持久化会话、消息幂等与商品切换。
- [x] 建立工具执行账本、确认绑定和未知效果处理。
- [ ] 用完全相同工具与预算对照 B2/B3，保留负结果。（共享工具工程对照已跑；完整效果实验未验收，因此整体任务保持未完成。）

- [ ] FR-15：冻结版本化意图库、标注规范与变化协议（§12.4–12.5）。（六类目录与基础协议已实现；独立标注规范及完整协议待补。）
- [ ] 实现带证据来源、版本幂等和合法迁移的多诉求状态管理（§12.6）。（基础更新器、会话持久化及定向测试已实现；完整约束/恢复场景未验收。）
- [ ] 接入受预算约束的结构化理解、会话持久化与规划输入；暂停/撤回约束覆盖提交前检查。
- [ ] 完成 INT-01 至 INT-12、人工复核真实多轮测试及三组消融，冻结效果和成本门槛后验收。

- [x] 八项客服范围：增加受主体/商品约束的模拟工单查询工具，只返回已有创建事实（CS-01/02/03 定向验证；见 [工单查询记录](docs/service_query.md)，不代表真实维修进度接入）。
- [ ] 八项客服范围：转人工交接记录与真实接收状态（CS-05）：**用户要求暂不实施**，不作为本轮工单查询任务。
- [ ] 完成申请→确认→提交→查询闭环及咨询不误办理、撤回阻断测试（CS-01/04/06）。

- [x] 主从框架首版：主控按需委派指南/排查/规则子 Agent，共享作用域、预算、账本并返回主控核验（实现见 §12.9）。
- [ ] 完成 MA-01 至 MA-05 全部验收，尤其真实路由、多诉求汇总与完整客服功能联验；保留直接工具路径。

- [ ] RAG 改造：将多源候选与精排接入既有 understand/Delta/主从流程，保持多诉求、未知出口、授权和总预算（§12.10）。
- [ ] RAG 改造：完成 BERT/意图精排训练数据与真实权重接入，以及 IR-01 至 IR-12 分层联验；未完成训练前不得称完整多路架构已实现。（BERT 开发版权重已训练/重载/接入测试；真实数据、意图精排训练及完整联验仍待完成。）

**退出条件**：I-07 至 I-14、E-01/E-03/E-05/E-08/E-10 核心用例通过；模型能根据反馈改变动作，而非仅按固定顺序执行；FR-15 还须满足 §12.8，不以历史 M4 测试替代新增验收。

### 19.7 M5：完整交互产品

**任务**

- [x] 实现 API、认证、上传和 SSE。
- [x] 实现商品栏、步骤卡片、原图与来源查看器。
- [x] 支持针对步骤追问、补图、切换商品、取消和恢复。
- [x] 加入 loading、缺图、无资料、等待澄清等 UI 状态。
- [x] 完成浏览器核心路径、越权和移动端测试（7 条浏览器用例；全部退出场景联验仍待完成）。

- [ ] FR-15：在现有消息流接入意图澄清与暂停/恢复交互；验证不把咨询变为办理、不重复收集有效信息，不用意图标签替代确认授权。

- [ ] 展示工单编号、实际状态、未查询到记录及交接状态，并完成 CS-01 至 CS-06 浏览器联验；模拟与真实环境标识清晰。

**退出条件**：E-01 至 E-12、I-17、相应安全用例通过；真实浏览器能看到可追溯原图，不只是命令行输出 Markdown 链接。

### 19.8 M6：ECom 原生环境适配

**任务**

- [x] 接入固定环境，保留原生工具语义和消息格式（模型 transport 为已登记的协议适配）。
- [x] 原生 tool calls 真实记录，兼容官方评分器读取路径。
- [x] 实现 trial 独立服务、数据副本、重置、清理和 watchdog。
- [ ] 冻结 dev/holdout 与模型配置，完成基线重跑（运行清单已冻结；20 次请求的真实冒烟已执行，完整公平基线仍待完成）。
- [ ] 运行预定任务/重复次数，输出 pass^k 及 action/search/output 分项。
- [ ] 核对数据库最终状态、工具记录和最终文本，不人工修改得分。

**退出条件**：I-15/I-16、B-08/B-09 通过；完整预定任务有结果，模拟业务确实执行，未把只读测试当全量评测。

### 19.9 M7：加固与发布

**任务**

- [ ] 完成安全、性能、恢复和旧功能全量回归（已有局部安全、恢复和旧功能回归证据；完整安全矩阵及正式性能尚未验收）。
- [ ] 冻结 PM209 正式 test 与 ECom 重复试验结果，完成公平消融。
- [ ] 审查报告、许可、敏感信息与假数据标识。
- [ ] 制作容器、依赖清单、部署说明、备份/回滚演练（本地候选、同版本恢复及 sg0004 跨基础镜像数据读取已验证；破坏性 schema 迁移回滚及新环境验收未完成）。
- [x] 完成本地容器浏览器验证、冷备份、进程强杀和恢复副本启动演练。
- [x] 更新 README：已实现能力、限制、可复现命令和真实小样例；未添加不存在的正式分数。
- [x] 审阅第 24 节验收证据并记录“不发布”决定；最终验收未通过。

**退出条件**：没有未关闭的严重安全/数据泄漏/来源伪造问题；所有发布门禁有实测证据；一台新环境可按文档完成演示与小规模复现。

---

<a id="s20"></a>
## 20. 开发环境、配置与命令约定

### 20.1 环境分层

`app-dev`：主应用、fake 测试、轻量真实数据库。

`inference`：Planner/VLM 独立服务，必要时按模型分开部署。

`mpmqa-reference`：旧基线与文字评分依赖隔离，不能污染主环境。

`ecom-reference`：固定 ECom 与 MCP/模型接口依赖，兼容补丁单独登记。

模型权重与数据不进入 Git。全局代理、外部模型路径由配置管理，不硬编码个人服务器地址。

### 20.2 当前仓库已有的起步命令

以下命令基于当前已核实的项目入口与 dev extra；需要在实际仓库目录执行，结果仍待 M0 验证。[S01][S02]

```bash
git status --short
git rev-parse HEAD
uv sync --locked --extra dev
uv run --no-sync pytest
uv run --no-sync shopguide --help
```

检查工作区后再选择独立分支/worktree。禁止为了“环境一致”执行破坏性 reset、清空数据或覆盖用户未提交代码。

### 20.3 新功能命令契约

**本小节命令是待实现接口，不是当前仓库已能执行的命令。对应功能完成后才加入 README 的 Quick Start。** 每条命令必须真实工作、失败返回非零退出码，禁止仅打印 success。

```bash
# 在新增 shopguide extra 后执行；首次解析依赖后提交更新的锁文件
uv sync --extra shopguide --extra dev

# 检查配置、数据库、模型能力和资源
uv run shopguide doctor --config configs/shopguide/dev.yaml

# PM209：检查原始数据并生成隔离的 corpus / eval_private
uv run shopguide data inspect --dataset pm209 --root data/raw/PM209
uv run shopguide data prepare --dataset pm209 \
  --raw-root data/raw/PM209 --out-root data/pm209

# 为允许的来源文档建立索引，gold 路径不能作为 corpus 输入
uv run shopguide index build --corpus data/pm209/corpus \
  --config configs/shopguide/pm209.yaml

# 导入授权演示手册；manifest 明确商品适用性和许可
uv run shopguide ingest manual --manifest data/demo/manuals.yaml \
  --config configs/shopguide/demo.yaml

# CLI 交互与 Web API
uv run shopguide chat --config configs/shopguide/demo.yaml
uv run shopguide serve --config configs/shopguide/demo.yaml
uv run shopguide worker --queue runs --config configs/shopguide/demo.yaml

# PM209：先生成不含 gold 的预测，再在隔离评分进程评分
uv run shopguide eval pm209 predict --split val \
  --profile pm209-agent-multipage --run-id pm209-val-b3
uv run shopguide eval pm209 score --run-id pm209-val-b3

# ECom：使用官方环境执行，不复用私有 mock 工具冒充原生环境
uv run shopguide eval ecom --profile ecom-reference \
  --num-trials 3 --run-id ecom-b3-n3

# 报告中合并诊断信息，不合并两套基准为一个“总准确率”
uv run shopguide report --run-id pm209-val-b3
```

`score` 子命令运行在拥有 gold 的隔离容器/进程中；在线 API 镜像不挂载 `eval_private`。`predict` 不接受 gold 参数；命令参数必须与实际实现的 schema 对齐。

### 20.4 计划的 Make 目标

| 目标 | 实际职责 |
|---|---|
| `make lint` | Ruff、类型检查和必要前端检查 |
| `make test-unit` | 单元测试并输出 JUnit/覆盖率 |
| `make test-contract` | API、tool schema、模型网关契约 |
| `make test-integration` | 真实本地 DB/索引/文件集成 |
| `make test-security` | 越权、注入、gold 隔离和上传测试 |
| `make test-metrics` | 评分兼容与边界样例测试 |
| `make test-e2e` | 启动测试应用后执行 Playwright |
| `make test-model-smoke` | 有预算授权时执行真实模型 smoke |
| `make test-legacy` | 旧视频项目全部回归 |
| `make report` | 从真实测试文件汇总报告 |
| `make release-check` | 全部门禁检查；缺报告或不一致即失败 |

模型/外部数据不可用时，相关目标应报明确 BLOCKED 或失败，不能零用例返回绿灯。离线目标不得偷偷下载模型或产生云费用。

### 20.5 配置模板

以下是完整字段组织的示例，模型 revision 和数据 manifest 仍需在 M0 填写；正式模式有缺项则拒绝运行。

```yaml
app:
  name: shopguide
  mode: demo
  schema_version: 2
  allow_real_business_writes: false
  allow_generated_images: false
storage:
  metadata_url: sqlite:///./.shopguide/metadata.db
  checkpoint_url: sqlite:///./.shopguide/checkpoints.db
  asset_root: ./.shopguide/assets
  lance_root: ./.shopguide/lancedb
  snapshot_id: null
models:
  planner:
    model_id: deepseek-v4-flash-vision-exp
    revision: null
    endpoint_env: SG_PLANNER_BASE_URL
    api_key_env: SG_PLANNER_API_KEY
  vision:
    model_id: deepseek-v4-flash-vision-exp
    revision: null
    endpoint_env: SG_VISION_BASE_URL
    api_key_env: SG_VISION_API_KEY
  verifier:
    model_ref: planner
    prompt_version: verifier-v1
retrieval:
  embedding_model: BAAI/bge-m3
  embedding_revision: null
  reranker_model: BAAI/bge-reranker-v2-m3
  reranker_revision: null
  candidates_per_channel: 30
  fused_candidate_limit: 60
  final_text_evidence_k: 8
  rrf_k: 60
  require_scope: true
budget:
  max_planner_steps: 8
  max_tool_calls: 12
  max_model_requests: 12
  reserved_finalization_requests: 2
  max_distinct_pages: 4
  max_image_inputs_per_call: 4
  max_image_inputs_per_turn: 8
  max_display_assets: 4
  context_window_tokens: 16384
  max_total_tokens_per_turn: 48000
  turn_deadline_seconds: 90
  request_timeout_seconds: 30
  max_transient_retries: 2
  max_parallel_read_tools: 2
security:
  demo_auth_local_only: true
  generated_image_tools_enabled: false
  arbitrary_network_tools_enabled: false
  max_upload_bytes: 20971520
  max_upload_pixels: 25000000
  require_asset_authorization: true
observability:
  log_level: INFO
  redact_secrets: true
  store_private_reasoning: false
  trace_sample_rate: 1.0
```

运行时 budget 的解析应检查预留值、上限和总时限关系，不能依赖模型遵守。ECom profile 单独定义协议预算，不继承此 demo profile 的限制。

---

<a id="s21"></a>
## 21. CI/CD、部署、运维与回滚

### 21.1 CI 流水线

```text
PR
 ├─ lint / type / schema
 ├─ unit + contract + metric fixtures
 ├─ legacy regression
 ├─ local DB/index integration
 ├─ security tests
 └─ browser core E2E

受信任分支或人工批准
 ├─ model capability smoke
 ├─ PM209 val smoke
 └─ ECom adapter smoke

发布候选
 ├─ 完整预定 benchmark + 配对报告
 ├─ 性能 / 故障注入 / 恢复 / 回滚
 ├─ 依赖漏洞与许可审计
 ├─ 镜像构建、签名/摘要与 SBOM
 └─ release-check
```

外部 PR 不获得云模型密钥、数据集私有下载凭证或部署权限。昂贵模型测试需显式触发和费用上限，不能每次格式调整都触发完整基准。GitHub Actions 第三方 action 版本在实施时固定可审核的 commit，不引用不受控分支。

### 21.2 单机部署拓扑

拟议 Compose 包含：`web`、`api`、`run-worker`，模型服务通过配置连接，`ingest-worker` 按需运行。业务数据、checkpoint、资产、索引使用独立持久卷。

第一版保证每个活跃会话单写、索引单写。API 多个并发连接不等于可任意启动多个 SQLite 写 worker；必须限制 worker 数、设置事务/锁等待策略并通过 P-03/P-06。更高并发时经 ADR 迁移业务数据库/checkpointer，并重复恢复和一致性测试。

推理服务不暴露公网；外部访问通过 HTTPS 反向代理。健康检查区分进程存活与系统是否能够接受新任务。模型暂时不可用时 `/ready` 和用户错误应一致，不让 API 一直返回成功入队却无法处理。

### 21.3 运行监控

记录以下指标，并按 `run_id/tool_call_id` 串联。日志里只保存必要参数或其脱敏/hash 版本。

| 类别 | 指标 |
|---|---|
| 请求 | 接收量、完成率、失败率、澄清/拒答/转人工比例 |
| 模型 | 各角色请求、token、图像输入、重试、超时、解析失败 |
| 检索 | scope 为空、索引错误、候选数、零命中、耗时 |
| 图像 | 缺图、跨型号阻断、来源校验失败、浏览器加载失败 |
| Agent | 步数、重复调用、预算耗尽、无进展中止、repair 类型 |
| 业务 | 模拟写入成功/失败/结果未知、重复抑制和确认过期 |
| 系统 | CPU/内存/GPU、磁盘、队列深度、文件句柄、DB 锁等待 |
| 质量 | 独立采样审核错误、用户配图反馈、版本冲突 |

告警初值由 smoke/load 测试决定，发布前登记。source validity、越权与重复业务写入出现异常属于高优先级；普通模型效果差与基础设施故障使用不同告警。

### 21.4 运行手册必须覆盖

- 模型 429/超时：查看 budget 与错误，不无限重试；必要时停止接单并明确提示。
- 图片 404：检查资产 registry、hash、卷挂载与权限；不能生成图兜底。
- 型号匹配错误：暂停受影响映射，撤销相关文档适用性并使缓存失效。
- 数据库/索引不一致：切回上一 ACTIVE snapshot，保留损坏代次供排查。
- 任务卡住：检查 lease、工具账本和截止时间；不要直接重做未知写操作。
- 磁盘不足：停止导入/新任务，保留活跃资料；清理只针对可回收代次与超期数据。
- 数据泄漏：立即停止相关服务/撤销凭证，按实际响应流程处理并保留最小审计材料。

### 21.5 备份与回滚

备份包含：数据库一致性快照、对应资产/索引代次、模型与配置 manifest、schema 版本。备份存在不等于可恢复；发布前进行一次恢复演练。

回滚顺序：停止新任务入队 → 完成或安全中止在途任务 → 备份当前状态 → 恢复兼容服务镜像和配置 → 切换已验证数据 snapshot → 健康检查与核心 E2E → 恢复流量。

数据库采用 expand/contract 迁移，先增加兼容字段再迁移使用，不能发布新服务后立即破坏旧服务所需字段。涉及不可逆迁移时必须有单独审批、备份和恢复路径。

### 21.6 发布工件

代码 tag、容器 digest、主/评测锁文件、模型 manifest、数据 manifest、提示版本、schema 版本、SBOM、测试报告、基准报告、已知限制、备份恢复记录、演示视频或操作说明。

公开的结果仅包含允许分发的去敏指标和必要示例；第三方原图许可不明确时不放进公开 README 截图。

---

<a id="s22"></a>
## 22. 可选的 SFT 与强化学习路线

**本节属于 P2，不阻塞首版。只有基础工具、数据、评测和安全边界稳定后才启动。**

本次意图候选增强中的 BERT/Cross-Encoder 监督训练依赖已审核意图数据，按 §8.11 分阶段执行；与这里可选的策略 SFT/RL 分开管理。没有标注与权重时，不提前宣称分类/精排分支可用。

### 22.1 训练前先回答的问题

B3 的错误主要来自检索、视觉识别、策略还是 schema？错误能否通过修复工具/资料解决？是否有独立训练数据与验证集？是否有足够预算采样多轮轨迹？没有这些条件时，不为增加关键词而训练。

### 22.2 优先顺序

```text
修复数据/工具与基线
 → 训练或优化区域重排序/检索（若是主要瓶颈）
 → 主控 SFT 学习合法调用与澄清
 → 有真实工具交互的策略 RL
```

先固定检索、视觉与核验工具，只训练主控，便于解释收益并控制成本。不要同时改底座、索引、提示、预算和训练方法后只报一个总提升。

### 22.3 SFT 数据

PM209 仅使用官方 train 及独立开发数据；可将授权的训练标注用于产生教师轨迹，但 test 的答案/区域绝不用于训练。电商业务轨迹使用独立合成任务和明确授权的记录，不把 ECom 全部公开任务拿来训练再报告同题成绩。

轨迹保存：允许的状态摘要、动作、实际 observation、答案、证据、审核结论和成本。检索/工具返回是环境观察，不当作策略自主生成的动作 token 计算同样的训练损失。

### 22.4 拟议 RL 奖励

下面是待实验的设计，不是已验证有效的公式：

```text
R = w1 * task_success
  + w2 * evidence_support
  + w3 * correct_visual_region_selection
  - w4 * normalized_tool_and_model_cost
  - w5 * unsupported_or_wrong_product_penalty
```

PM209 与 ECom 分别定义对应奖励，不能把两个基准的不同分数简单拼为同一标量后宣称统一效果。商品权限与禁止生成图属于硬约束，不交给奖励权重讨价还价。

### 22.5 防奖励投机

- 只因关键词满足不能证明语义正确，加入独立抽查，不修改官方测试分数。
- 返回全部区域或整页，需受到 precision 与冗余成本约束。
- 全部拒答，需通过 coverage 与任务成功率暴露。
- 自身核验通过不能作为唯一奖励。
- 减少调用不得以跳过必要确认、跨型号检索或省略关键警告为代价。
- 更新策略参数的过程、训练步数、数据与检查点要真实存在；打分但不更新参数不称为 RL。

### 22.6 训练验收

对比同模型 Prompt → SFT → RL，在独立测试任务上报告正确性、图文证据、成本和稳定性。训练算法、实现库与其版本在启动前通过新的 ADR 固定，禁止为了本计划完整性编造单卡显存和训练时长。

---

<a id="s23"></a>
## 23. 风险登记与资源计划

### 23.1 风险表

| 风险 | 触发信号 | 应对与阻塞范围 |
|---|---|---|
| PM209 下载不可达 | 无法取得完整数据/压缩包损坏 | 记录失败并联系官方；阻塞正式评测，工程 fixtures 不替代 |
| 旧 MPMQA 依赖不兼容 | scorer/模型无法安装 | 隔离环境、最小兼容补丁、parity；不得偷偷换指标 |
| ECom 服务模型不可用 | 原用户/视觉端点报错 | 明确替代 profile，全部比较方法重跑，标注不可直接与原表对比 |
| 原图/资料授权不清 | 许可无法覆盖公开展示 | 仅保留合法研究使用；公开演示使用授权资料 |
| 手册没有所需操作图 | 只有文字或不清晰页面 | 有依据文字 + 明确缺图，不生成示意图 |
| 型号/版本元信息不足 | 系列型号混杂 | 限制支持范围、补充人工映射或追问 |
| 解析导致图片误裁 | 旋转、偏移或标签缺失 | 坐标回归、整页入口、保留 transform |
| 检索候选泄露其他商品 | scope 前置过滤失败 | 阻塞发布；实现受限分区，禁止只用 postfilter |
| 多轮状态串扰 | 会话/任务对象错乱 | 主体隔离、revision、单会话锁与商品切换测试 |
| 工具重试重复业务效果 | 响应丢失后重复提交 | 幂等账本、结果查询、OUTCOME_UNKNOWN |
| Agent 只增加成本 | B3 无稳定效果增益 | 默认保留 B2；定位问题再优化，不强行上 RL |
| 数据污染 | 测试标签出现在 corpus/轨迹 | 清理、重建、重新评测，已污染结果失效 |
| 小基准过拟合 | 反复在相同 53 任务调参 | 合成 dev、冻结 holdout、公开已调试范围 |
| 硬件/预算不足 | OOM、费用超额或高延迟 | 分离模型服务、降低并发/输入预算，作为新 profile 记录 |
| “未跑但看似支持” | README 有命令，实际无实现 | CLI/Make 契约测试、release-check 阻断 |

### 23.2 资源分级

| 阶段 | 资源计划 | 边界 |
|---|---|---|
| 离线开发 | 普通开发机，本地 SQLite/文件/小索引 | 不运行真实大模型，不能据此评测模型效果 |
| 模型 smoke | 可访问的真实 Planner/VLM 服务 + 小规模样本 | 先计量 token、图像和延迟 |
| 本地推理 | 根据实际 GPU 部署单独服务 | 不承诺两个模型能同时放入某张未确认显卡 |
| 完整基准 | 数据存储 + 并发受控的模型请求预算 | ECom 还包含模拟用户和视觉工具成本 |
| SFT/RL | 独立预算、训练设备和开发集 | P2 单独评估，不计入首版必须交付 |

存储计划先读取数据包实际大小，并预留原始、渲染、索引、缓存与结果空间；渲染空间与页数、分辨率和图块数有关，不能凭论文问题数量估算。

### 23.3 费用核算

按每个模型记录输入/输出 token、图像输入、重试与耗时；使用外部服务时从当时的正式价目填写成本表，不把过期价格写成固定事实。本地推理报告 GPU 时间与设备配置，不能直接和云账单混为一个单位。

正式评测前运行小规模 pilot，计算预计总调用量与上限，项目负责人确认预算后再运行；超额自动停止并保存可恢复状态。

2026-09-10 用户补充授权：“批准，五十次以下的都不用问我”。后续 50 次以下小规模验证
按预先记录的硬上限执行，不重复请求确认；更大实验仍需整体预算批准，不能拆批规避。

---

<a id="s24"></a>
## 24. 最终验收、交付清单与协作规则

### 24.1 需求到测试的追踪

| 需求 | 最低测试覆盖 |
|---|---|
| FR-01 商品/订单 | U-02、I-03、S-01、E-01、E-05 |
| FR-02 资料入库 | U-03/04/05/06、I-01/02/05/06 |
| FR-03 检索 | I-03/04、B-01/04、PM209 retrieval、IR-09/10/11/12；§8.10 证据 RAG 改造 |
| FR-04 图文答案 | I-07/08/09、E-02/06、PM209 text/region |
| FR-05 多轮 | I-11/12、E-03/04/05/08/10 |
| FR-06 主动决策 | U-11/12、I-18、B2/B3 配对实验 |
| FR-07 核验 | U-13、I-07、S-02/08/09、E-07 |
| FR-08 PM209 | U-14/15、B-01 至 B-07、完整报告 |
| FR-09 ECom | U-16、I-15/16、B-08/09、完整报告 |
| FR-10 观测 | I-17/18、P-01 至 P-08、run 工件审计 |
| FR-11 用户图片 | I-10、S-04/07、E-04/11 |
| FR-12 视频 | 视频 adapter 回归与单独时间/帧引用测试 |
| FR-13 模拟工单及售后闭环 | I-13/14、E-09/10、S-10、CS-01 至 CS-06；八项能力边界见 §1.2.1 |
| FR-14 训练扩展 | P2 独立训练协议、数据审计和消融 |
| FR-15 意图感知 | INT-01 至 INT-12、IR-01 至 IR-08/11/12、§8.8–8.11/§12.8/§12.10 分层评测及接入 |

### 24.2 发布前必须全部核对

- [ ] 新功能已实现，不仅是文档和界面占位。
- [ ] FR-15 意图库/协议版本、状态迁移审计、真实多轮评测及暂停撤回授权负例完成验收。
- [ ] 原项目命令与测试保持兼容，旧表未被覆盖。
- [ ] 用户能够选择商品，并完成带原图的使用方法问答。
- [ ] 原图来自登记来源，型号、页码、版本与区域可核对。
- [ ] 生成图片、任意 URL 图片替补和未授权网络工具未启用。
- [ ] 多轮指代、商品切换、缺资料、缺图和补拍有可运行处理流程。
- [ ] 所有模型、工具和资产错误有界，不会无限循环。
- [ ] 模拟写操作有实际执行记录；确认和幂等有效。
- [ ] PM209 given-page、retrieved-top1、multipage 指标分开。
- [ ] ECom 使用官方任务/工具/评分，失败和超时包含在报告。
- [ ] official 与扩展指标名称、分母、协议和版本清楚。
- [ ] 没有把测试答案、gold 区域或隐藏任务 metadata 暴露给 Agent。
- [ ] fake 测试结果没有进入效果表。
- [ ] B2/B3 使用相同条件；负结果和成本也保留。
- [ ] 安全/越权/原图伪造问题全部关闭。
- [ ] 部署、健康检查、数据备份、恢复和回滚演练完成。
- [ ] README 只写真实能力、实测指标和可运行命令。
- [ ] 数据、模型、第三方代码和演示图片的许可已分别检查。

### 24.3 交付物清单

代码：ShopGuide 模块、兼容层、前端、迁移、配置、CLI、Make 目标和 CI。

数据工具：下载说明、审计、corpus/gold 隔离转换、索引构建、来源映射与版本清单。

测试：本计划矩阵对应的自动测试、fixtures、真实模型 smoke、评分 parity 与报告。

评测：PM209 独立报告、ECom 独立报告、B2/B3 消融、成本和失败案例。

部署：镜像/Compose、环境模板、初始化步骤、runbook、备份与回滚。

说明：架构图的文字版本或后续图示、API 契约、已知限制、许可清单、演示操作说明。

### 24.4 开发人员或代码 Agent 的执行规则

1. 从 M0 开始，按依赖顺序推进；不要一次提交全部模块而没有中间验收。
2. 每个任务先写验收条件，再修改代码；完成后提供修改文件、运行命令、真实结果和未解决项。
3. “计划”“已实现”“已测试”“已评测”分开记录。
4. 不得为了通过测试删掉测试、放宽权限、硬编码答案或隐藏异常。
5. 不在未知真实商品上编造说明书内容、型号映射或图片出处。
6. 不把新功能偷偷接入真实商城账号、付款、退款或外部写操作。
7. 不更改测试 split、评分器语义或数据范围而不更新协议。
8. 依赖变更与锁文件一起提交；模型服务与主服务环境分别验证。
9. 涉及 schema、工具语义、预算或默认模型的变化必须更新 ADR、测试与 manifest。
10. 遇到数据/服务/许可阻塞时记录 BLOCKED，不用模拟分数冒充真实结果。
11. 用户约定（2026-09-11）：修复完成后直接执行相应测试，不再询问“是否开始测试”。先运行必要的离线回归，再按既定范围和已授权预算执行真实模型复验；涉及 API/界面的变更补做相应联验。测试失败时继续定位修复并复测，保留各轮失败，不以定向通过替代全量验收。此约定不自动取消已明确的累计请求上限；额度不足或服务不可用时记录未执行项，继续完成可独立执行的检查。

### 24.5 第一轮开发应执行的最小任务包

只执行 M0 和 M1：保存基线 → 审计两套数据 → 验证评分器 → 明确模型服务 → 建立 schema/fixtures/CI。第一轮不要先开发 RL、多 Agent 编排或复杂前端。

第一轮结束必须交付可检查的审计报告和测试结果，再进入原图入库与图文问答。项目最终亮点应由“型号正确、步骤有据、配图真实、交互有效、评测可复现”共同支撑。

---

<a id="s25"></a>
## 25. 资料来源与版本登记

所有外部资料查阅日期为 **2026-09-09**。下列链接用于核查事实和实施时固定依赖，不表示资料已全部下载、运行或具备商业再分发授权。

### 25.1 已核实源码快照

| 仓库 | 分支 | 编制时 SHA |
|---|---|---|
| `ccav1yq7/ShopGuide` | main | `fe4050d1c228c0dd4ecd5d37c4f77b2b9dd395d2` |
| `AIM3-RUC/MPMQA` | master | `5226a9aa849fd3b8f35620e7b7d7d10c09d754a2` |
| `XiaoduoAILab/ECom-Bench` | main | `bc5018daaf45eea12330941bce68cebd293cfa85` |

实施时可以选择更新版本，但必须记录原因、差异和新的测试结果，不能仍使用旧 snapshot 名称。模型 revision、数据压缩包 SHA 与依赖锁版本尚待 M0 确认。

### 25.2 引用索引

**[S01] LayoutTraceRAG README（旧方向，已舍弃）。** 当时的能力、历史评测限制、安装与运行说明。

`https://github.com/ccav1yq7/LayoutTraceRAG/blob/fe4050d1c228c0dd4ecd5d37c4f77b2b9dd395d2/README.md`

**[S02] LayoutTraceRAG pyproject（旧方向，已舍弃）。** 包名、版本、Python 下限、依赖与 CLI 入口。

`https://github.com/ccav1yq7/LayoutTraceRAG/blob/fe4050d1c228c0dd4ecd5d37c4f77b2b9dd395d2/pyproject.toml`

**[S03] 现有存储与检索连接。** 视频字段持久化、测试词法路径和 dense/FTS 查询。

`https://github.com/ccav1yq7/LayoutTraceRAG/blob/fe4050d1c228c0dd4ecd5d37c4f77b2b9dd395d2/layouttrace/index/store.py`

**[S04] 现有证据类型。** 视频节点与 Citation 的字段约束。

`https://github.com/ccav1yq7/LayoutTraceRAG/blob/fe4050d1c228c0dd4ecd5d37c4f77b2b9dd395d2/layouttrace/types.py`

**[S05] 现有图节点。** 充分性判断、重生成和引用核验。

`https://github.com/ccav1yq7/LayoutTraceRAG/blob/fe4050d1c228c0dd4ecd5d37c4f77b2b9dd395d2/layouttrace/graph/nodes.py`

**[S06] 现有 LLM、CLI 与评测。** 文本证据格式、单轮入口和视频评测口径。

`https://github.com/ccav1yq7/LayoutTraceRAG/blob/fe4050d1c228c0dd4ecd5d37c4f77b2b9dd395d2/layouttrace/llm/langchain_engine.py`

`https://github.com/ccav1yq7/LayoutTraceRAG/blob/fe4050d1c228c0dd4ecd5d37c4f77b2b9dd395d2/layouttrace/cli.py`

`https://github.com/ccav1yq7/LayoutTraceRAG/blob/fe4050d1c228c0dd4ecd5d37c4f77b2b9dd395d2/layouttrace/eval/harness.py`

**[S07] MPMQA: Multimodal Question Answering on Product Manuals，AAAI 2023。** PM209 的任务、规模和图文答案定义。

`https://ojs.aaai.org/index.php/AAAI/article/view/26634`

**[S08] MPMQA 官方仓库与 README。** 数据下载入口、URA、旧环境及训练/评测脚本。

`https://github.com/AIM3-RUC/MPMQA/tree/5226a9aa849fd3b8f35620e7b7d7d10c09d754a2`

**[S09] PM209 官方 loader。** 数据路径、页面、区域、问答与相关区域字段。

`https://github.com/AIM3-RUC/MPMQA/blob/5226a9aa849fd3b8f35620e7b7d7d10c09d754a2/dataset/mqa_dataset.py`

**[S10] PM209 区域类别。** 六类语义区域与细粒度图像类别。

`https://github.com/AIM3-RUC/MPMQA/blob/5226a9aa849fd3b8f35620e7b7d7d10c09d754a2/dataset/const.py`

**[S11] ECom-Bench，EMNLP 2025 Industry Track；作者论文 HTML。** 任务范围、论文版配置、pass^k 和局限。

`https://aclanthology.org/2025.emnlp-industry.19/`

`https://arxiv.org/html/2507.05639v2`

**[S12] ECom 官方环境实现。** trial 数据副本、工具轨迹读取、reward 组合及运行流程。

`https://github.com/XiaoduoAILab/ECom-Bench/blob/bc5018daaf45eea12330941bce68cebd293cfa85/envs/story/env.py`

`https://github.com/XiaoduoAILab/ECom-Bench/blob/bc5018daaf45eea12330941bce68cebd293cfa85/README.md`

**[S13] LangGraph Persistence。** checkpointer、线程、存储与恢复。

`https://docs.langchain.com/oss/python/langgraph/persistence`

**[S14] LangGraph Interrupts。** 中断恢复、节点重放与副作用注意事项。

`https://docs.langchain.com/oss/python/langgraph/interrupts`

**[S15] LanceDB Metadata Filtering。** 检索范围过滤及相应接口。

`https://docs.lancedb.com/search/filtering`

**[S16] pdfplumber。** 文本、表格、坐标与裁剪操作参考。

`https://github.com/jsvine/pdfplumber`

**[S17] pypdfium2。** 页面渲染和 PDF 坐标操作参考。

`https://pypdfium2.readthedocs.io/`

**[S18] Qwen3-8B 官方模型卡。** 所选 Planner 基线与非思考模式配置。

`https://huggingface.co/Qwen/Qwen3-8B`

**[S19] Qwen2.5-VL-7B-Instruct 官方模型卡。** 所选视觉观察基线与图像输入。

`https://huggingface.co/Qwen/Qwen2.5-VL-7B-Instruct`

**[S20] MPMQA 官方评测实现。** 页面检索、区域聚合、文字预处理及预测格式。

`https://github.com/AIM3-RUC/MPMQA/blob/5226a9aa849fd3b8f35620e7b7d7d10c09d754a2/evaluate.py`

---

## 决策摘要

**产品：**已购商品的使用指导和售后图文问答。

**架构：**一个主控 Agent + 受控工具 + 硬性型号/权限约束 + 原图证据 + 强制核验。

**改造：**保留旧 LayoutTraceRAG，在新子模块中扩展文档/商品类型与存储，不破坏视频链路。

**评测：**PM209 衡量说明书检索、回答与区域选择；ECom-Bench 衡量多轮工具任务，两个报告独立。

**顺序：**基线与数据 → 契约 → 入库检索 → 固定图文 RAG → Agent → 前端 → ECom → 加固与发布。

**底线：**不生成商品配图，不跨型号猜操作，不泄露测试答案，不伪造运行结果，不把演示系统当作真实商城生产系统。
